<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Kahve ERP Ultra Pro | Kurumsal Premium</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf-autotable/3.5.23/jspdf.plugin.autotable.min.js"></script>
<style>
/* GLOBAL & GÖRSEL MOTOR */
*{margin:0;padding:0;box-sizing:border-box;}
:root{
    --primer:#6F4E37; --sekonder:#2C5F7C; --accent:#D4855C;
    --bg:#F8F6F3; --card:rgba(255,255,255,0.88);
    --text:#2D2D2D; --border:rgba(224,220,216,0.5);
    --glass:rgba(255,255,255,0.75);
    --success:#2E7D32; --danger:#C62828; --warn:#E65100;
    --gray:#888;
}
body{font-family:'Segoe UI',system-ui,sans-serif;background:#F8F6F3;color:var(--text);overflow:hidden;}

/* PREMIUM ARKA PLANLAR */
.page{display:none;height:calc(100vh - 65px);padding:2rem;overflow-y:auto;background-size:cover;background-position:center;background-attachment:fixed;}
.page.active{display:block;animation:pageFade 0.6s ease;}
@keyframes pageFade{from{opacity:0;transform:translateY(10px);}to{opacity:1;transform:translateY(0);}}

#page-dashboard{background-image:linear-gradient(rgba(248,246,243,0.75),rgba(248,246,243,0.75)),url('https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?q=80&w=1920');}
#page-satis{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1447933601403-0c6688de566e?q=80&w=1920');}
#page-urunler{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1611854779393-1b2da9d400fe?q=80&w=1920');}
#page-musteriler{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1501339847302-ac426a4a7cbb?q=80&w=1920');}
#page-ziyaret{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1554118811-1e0d58224f24?q=80&w=1920');}
#page-raporlar{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1504630083234-14187a9df0f5?q=80&w=1920');}
#page-klasman{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1521302200778-33500795e128?q=80&w=1920');}
#page-cari{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?q=80&w=1920');}
#page-admin{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1464983953574-0892a716854b?q=80&w=1920');}
#page-islemler{background-image:linear-gradient(rgba(255,255,255,0.82),rgba(255,255,255,0.82)),url('https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?q=80&w=1920');}

/* LOGIN SCREEN */
#login-screen{
    position:fixed;inset:0;
    background:linear-gradient(rgba(0,0,0,0.55),rgba(0,0,0,0.55)),url('https://images.unsplash.com/photo-1509042239860-f550ce710b93?q=80&w=1920') center/cover;
    display:flex;align-items:center;justify-content:center;z-index:10000;
}
.login-box{
    background:rgba(255,255,255,0.93);padding:3.5rem;border-radius:30px;
    width:430px;box-shadow:0 30px 60px rgba(0,0,0,0.45);backdrop-filter:blur(18px);text-align:center;
}
.login-box h2{color:var(--primer);font-size:32px;font-weight:800;margin-bottom:6px;}
.login-box p{color:#8B8B8B;margin-bottom:2rem;font-size:13px;}
.login-box input{width:100%;padding:15px;margin-bottom:14px;border-radius:12px;border:1.5px solid #ddd;outline:none;font-size:15px;transition:0.3s;}
.login-box input:focus{border-color:var(--accent);box-shadow:0 0 10px rgba(212,133,92,0.2);}

/* HEADER */
.header{background:rgba(111,78,55,0.96);color:#fff;padding:1.2rem 2.5rem;display:flex;justify-content:space-between;align-items:center;backdrop-filter:blur(10px);z-index:1000;position:relative;}
.layout{display:flex;height:calc(100vh - 65px);}
.sidebar{width:270px;background:var(--card);border-right:1px solid var(--border);backdrop-filter:blur(15px);overflow-y:auto;}
.sidebar nav a{display:flex;align-items:center;gap:13px;padding:14px 26px;color:var(--text);text-decoration:none;cursor:pointer;transition:0.3s;border-left:5px solid transparent;font-size:13px;font-weight:500;}
.sidebar nav a:hover,.sidebar nav a.active{background:rgba(111,78,55,0.08);border-left-color:var(--accent);color:var(--primer);font-weight:700;}

/* TABLES & CARDS */
.glass-panel{background:var(--glass);border-radius:20px;box-shadow:0 15px 35px rgba(0,0,0,0.05);border:1px solid rgba(255,255,255,0.3);padding:1.5rem;margin-top:1.5rem;}
table{width:100%;border-collapse:collapse;}
th{text-align:left;padding:16px;font-size:11px;text-transform:uppercase;color:var(--gray);letter-spacing:1px;border-bottom:1.5px solid var(--border);}
td{padding:16px;border-bottom:1px solid rgba(0,0,0,0.03);font-size:13px;vertical-align:middle;}
tr:last-child td{border-bottom:none;}
tr:hover td{background:rgba(111,78,55,0.03);}

/* BUTTONS */
.btn{padding:9px 18px;border:none;border-radius:10px;cursor:pointer;font-weight:700;font-size:12px;display:inline-flex;align-items:center;gap:7px;transition:0.3s;}
.btn:hover{transform:translateY(-1px);filter:brightness(1.1);}
.btn-primary{background:var(--primer);color:#fff;}
.btn-secondary{background:var(--sekonder);color:#fff;}
.btn-wa{background:#25D366;color:#fff;}
.btn-danger{background:#f44336;color:#fff;}
.btn-warn{background:#FF8C00;color:#fff;}
.btn-success{background:#388E3C;color:#fff;}
.btn-sm{padding:6px 12px;font-size:11px;}

/* MODALS */
.modal-bg{position:fixed;inset:0;background:rgba(0,0,0,0.7);display:none;align-items:center;justify-content:center;z-index:9000;backdrop-filter:blur(8px);}
.modal{background:#fff;padding:3rem;border-radius:25px;width:780px;max-height:90vh;overflow-y:auto;box-shadow:0 40px 80px rgba(0,0,0,0.5);border:1px solid rgba(255,255,255,0.2);}
.modal-sm{background:#fff;padding:2.5rem;border-radius:25px;width:520px;max-height:90vh;overflow-y:auto;box-shadow:0 40px 80px rgba(0,0,0,0.5);}
.modal-lg{background:#fff;padding:3rem;border-radius:25px;width:950px;max-height:90vh;overflow-y:auto;box-shadow:0 40px 80px rgba(0,0,0,0.5);}
.fg{margin-bottom:1.4rem;display:flex;flex-direction:column;gap:7px;}
.fg label{font-size:11px;font-weight:800;color:var(--primer);text-transform:uppercase;letter-spacing:0.5px;}
.fg input,.fg select,.fg textarea{padding:12px 15px;border:1.5px solid #eee;border-radius:12px;font-size:14px;transition:0.3s;font-family:inherit;}
.fg input:focus,.fg select:focus,.fg textarea:focus{border-color:var(--primer);outline:none;}

/* SMART ALERTS */
.alert-bar{padding:14px 20px;border-radius:14px;margin-bottom:12px;display:flex;align-items:center;gap:12px;font-size:13px;font-weight:600;animation:slideIn 0.4s ease;}
.alert-red{background:rgba(198,40,40,0.1);border-left:5px solid #C62828;color:#C62828;}
.alert-orange{background:rgba(230,81,0,0.1);border-left:5px solid #E65100;color:#E65100;}
.alert-blue{background:rgba(44,95,124,0.12);border-left:5px solid #2C5F7C;color:#2C5F7C;}
@keyframes slideIn{from{transform:translateX(-10px);opacity:0;}to{transform:translateX(0);opacity:1;}}

/* BADGES */
.badge{padding:4px 12px;border-radius:30px;font-size:10px;font-weight:800;letter-spacing:0.5px;}
.badge-vip{background:linear-gradient(135deg,#FFD700,#FFA000);color:#000;}
.badge-premium{background:linear-gradient(135deg,#6F4E37,#A0522D);color:#fff;}
.badge-standart{background:#e0e0e0;color:#555;}
.badge-evrak{background:rgba(44,95,124,0.15);color:var(--sekonder);}
.badge-fatura{background:rgba(46,125,50,0.15);color:#2E7D32;}
.badge-irsaliye{background:rgba(230,81,0,0.15);color:#E65100;}

/* KLASMAN CARDS */
.klasman-card{background:var(--glass);border-radius:18px;padding:1.5rem;border:1px solid rgba(255,255,255,0.4);box-shadow:0 10px 25px rgba(0,0,0,0.07);transition:0.3s;}
.klasman-card:hover{transform:translateY(-3px);box-shadow:0 15px 35px rgba(0,0,0,0.12);}

/* PROGRESS BAR */
.prog-bar{background:#eee;border-radius:10px;height:8px;overflow:hidden;margin-top:5px;}
.prog-fill{height:100%;border-radius:10px;transition:width 0.6s ease;}

/* TABS */
.tab-btns{display:flex;gap:8px;margin-bottom:1.5rem;}
.tab-btn{padding:10px 22px;border:2px solid var(--border);border-radius:12px;cursor:pointer;font-weight:600;font-size:13px;background:transparent;transition:0.3s;}
.tab-btn.active{background:var(--primer);color:#fff;border-color:var(--primer);}

/* STAT CARDS */
.stat-card{background:#fff;padding:1.4rem;border-radius:15px;border-bottom:4px solid var(--primer);box-shadow:0 5px 20px rgba(0,0,0,0.06);}
.stat-card small{font-weight:700;color:var(--gray);font-size:10px;text-transform:uppercase;letter-spacing:1px;}
.stat-card h2{font-size:24px;margin-top:5px;}

/* SCROLLBAR */
::-webkit-scrollbar{width:6px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:rgba(111,78,55,0.3);border-radius:3px;}

/* SECTION TITLE */
.section-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:0.5rem;}
.section-head h2{font-size:22px;font-weight:800;color:var(--primer);}

/* DROPDOWN CATEGORY FILTER */
.cat-filter{display:flex;gap:10px;flex-wrap:wrap;margin:1rem 0;}
.cat-btn{padding:7px 16px;border-radius:30px;border:1.5px solid var(--border);background:transparent;cursor:pointer;font-size:12px;font-weight:600;transition:0.3s;}
.cat-btn.active{background:var(--primer);color:#fff;border-color:var(--primer);}

/* CARI BALANCE */
.cari-pos{color:var(--success);font-weight:700;}
.cari-neg{color:var(--danger);font-weight:700;}
.cari-zero{color:var(--gray);font-weight:600;}
</style>
</head>
<body>

<!-- ===== LOGIN SCREEN ===== -->
<div id="login-screen">
    <div class="login-box">
        <div style="font-size:48px;margin-bottom:10px;">☕</div>
        <h2 style="font-family:'Georgia',serif;">Kahve ERP Ultra</h2>
        <p>Kurumsal Yönetim Paneli | v3.0 Pro</p>
        <input type="text" id="log-u" placeholder="Kullanıcı Adı" autocomplete="username">
        <input type="password" id="log-p" placeholder="Şifre" autocomplete="current-password" onkeydown="if(event.key==='Enter')core.login()">
        <button class="btn btn-primary" style="width:100%;justify-content:center;padding:17px;font-size:15px;border-radius:15px;" onclick="core.login()">🔐 Sisteme Giriş Yap</button>
        <div style="margin-top:1.5rem;font-size:11px;color:#aaa;background:#f8f8f8;padding:10px;border-radius:10px;">Demo — Admin: <b>admin</b> | Şifre: <b>1234</b></div>
    </div>
</div>

<!-- ===== ŞİFRE DEĞİŞTİRME EKRANI ===== -->
<div id="sifre-screen" style="display:none;position:fixed;inset:0;background:linear-gradient(rgba(0,0,0,0.6),rgba(0,0,0,0.6)),url('https://images.unsplash.com/photo-1509042239860-f550ce710b93?q=80&w=1920') center/cover;align-items:center;justify-content:center;z-index:9999;">
    <div class="login-box" style="width:460px;">
        <div style="font-size:40px;margin-bottom:10px;">🔑</div>
        <h2 style="font-family:'Georgia',serif;font-size:24px;">Yeni Şifre Oluştur</h2>
        <p id="sifre-aciklama" style="color:#888;margin-bottom:2rem;font-size:13px;line-height:1.6;">İlk girişiniz tespit edildi. Güvenliğiniz için lütfen yeni bir şifre belirleyin.</p>
        <div style="background:rgba(111,78,55,0.08);border-radius:14px;padding:14px;margin-bottom:1.5rem;text-align:left;font-size:12px;color:#666;">
            <b style="color:var(--primer);">Şifre Kuralları:</b><br>
            ✅ En az 6 karakter<br>
            ✅ En az 1 rakam içermeli<br>
            ✅ Eski şifrenizden farklı olmalı
        </div>
        <input type="password" id="sifre-yeni" placeholder="Yeni Şifre" autocomplete="new-password" oninput="core.sifreKontrol()">
        <input type="password" id="sifre-tekrar" placeholder="Yeni Şifre (Tekrar)" autocomplete="new-password" oninput="core.sifreKontrol()">
        <div id="sifre-msg" style="font-size:12px;margin-bottom:12px;min-height:18px;font-weight:600;"></div>
        <button id="sifre-btn" class="btn btn-primary" style="width:100%;justify-content:center;padding:17px;font-size:15px;border-radius:15px;opacity:0.5;" onclick="core.sifreKaydet()" disabled>✅ Şifremi Güncelle</button>
        <div style="margin-top:1rem;font-size:11px;color:#aaa;">Bu adımı geçemezsiniz — sistem güvenliği için zorunludur.</div>
    </div>
</div>

<!-- ===== HEADER ===== -->
<header class="header">
    <div style="display:flex;align-items:center;gap:15px;">
        <div style="background:#fff;border-radius:10px;padding:5px 8px;"><span style="font-size:22px;">☕</span></div>
        <h1 style="font-size:20px;letter-spacing:1px;font-weight:800;">KAHVE <span style="font-weight:200;">ERP ULTRA</span></h1>
    </div>
    <div style="display:flex;align-items:center;gap:15px;">
        <div id="alert-count" style="display:none;background:#f44336;color:#fff;border-radius:30px;padding:5px 14px;font-size:12px;font-weight:700;cursor:pointer;" onclick="core.nav('dashboard')">🔔 <span id="alert-num">0</span> Uyarı</div>
        <div id="userBadge" style="background:rgba(255,255,255,0.2);padding:6px 18px;border-radius:30px;font-size:12px;font-weight:600;">Bekleniyor...</div>
        <button class="btn" style="background:rgba(255,255,255,0.15);color:#fff;font-size:12px;" onclick="core.logout()">🚪 Çıkış</button>
    </div>
</header>

<!-- ===== LAYOUT ===== -->
<div class="layout">
    <aside class="sidebar">
        <nav>
            <a onclick="core.nav('dashboard')" class="active">📊 Dashboard</a>
            <a onclick="core.nav('islemler')">⚡ İşlem Takibi</a>
            <a onclick="core.nav('satis')">🛒 Satışlar & Cari</a>
            <a onclick="core.nav('urunler')">📦 Stok & Depo</a>
            <a onclick="core.nav('ziyaret')">🗓️ Saha & Ziyaret</a>
            <a onclick="core.nav('musteriler')">👥 Müşteri Rehberi</a>
            <a onclick="core.nav('klasman')">🏆 Müşteri Klasmanı</a>
            <a onclick="core.nav('cari')">💰 Cari Hesaplar</a>
            <a onclick="core.nav('raporlar')">📈 Finansal Analiz</a>
            <a id="nav-adm" onclick="core.nav('admin')" style="display:none;color:var(--danger);border-top:1px solid #eee;margin-top:10px;">⚙️ Admin Paneli</a>
        </nav>
    </aside>

    <main style="flex:1;position:relative;overflow:hidden;">

        <!-- DASHBOARD -->
        <div id="page-dashboard" class="page active">

            <!-- BÖLÜM 1: İSTATİSTİK KARTI (EN ÜSTTE) -->
            <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:1rem;margin-bottom:1rem;" id="dash-stats"></div>

            <!-- BÖLÜM 2: SON SATIŞLAR + İŞLEMLER (ORTADA) -->
            <div style="display:grid;grid-template-columns:1.6fr 1fr;gap:1.2rem;margin-bottom:1.2rem;">
                <div class="glass-panel" style="margin-top:0;">
                    <h3 style="margin-bottom:1rem;color:var(--primer);">📋 Son Satış Hareketleri</h3>
                    <table id="dash-last-sales"><thead><tr><th>Tarih</th><th>Müşteri</th><th>Tutar</th><th>Evrak</th></tr></thead><tbody></tbody></table>
                </div>
                <div class="glass-panel" style="margin-top:0;">
                    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem;">
                        <h3 style="color:var(--sekonder);">⚡ İşlemler</h3>
                        <button class="btn btn-primary btn-sm" onclick="core.openIslemModal()">+ Ekle</button>
                    </div>
                    <div class="tab-btns" style="margin-bottom:1rem;flex-wrap:wrap;gap:5px;">
                        <button class="islem-tab tab-btn active" style="padding:6px 12px;font-size:11px;" onclick="core.filterIslem('bekleyen',this)">⏳ Bekleyen</button>
                        <button class="islem-tab tab-btn" style="padding:6px 12px;font-size:11px;" onclick="core.filterIslem('tamamlandi',this)">✅ Biten</button>
                        <button class="islem-tab tab-btn" style="padding:6px 12px;font-size:11px;" onclick="core.filterIslem('tum',this)">📋 Tümü</button>
                    </div>
                    <div id="islem-listesi" style="max-height:340px;overflow-y:auto;"></div>
                </div>
            </div>

            <!-- BÖLÜM 3: UYARILAR (EN ALTTA) -->
            <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:1.2rem;margin-bottom:1rem;">
                <div class="glass-panel" style="margin-top:0;">
                    <h3 style="margin-bottom:1rem;color:var(--danger);">🚨 Kritik Stok</h3>
                    <div id="dash-low-stock"></div>
                </div>
                <div class="glass-panel" style="margin-top:0;">
                    <h3 style="margin-bottom:1rem;color:var(--warn);">📦 Müşteri Stok Uyarıları</h3>
                    <div id="dash-customer-alerts"></div>
                </div>
                <div class="glass-panel" style="margin-top:0;">
                    <h3 style="margin-bottom:1rem;color:var(--sekonder);">🔔 Akıllı Uyarılar</h3>
                    <div id="smart-alerts"></div>
                </div>
            </div>

        </div>

        <!-- İŞLEMLER SAYFASI (Sidebar'dan ayrı tam sayfa) -->
        <div id="page-islemler" class="page">
            <div class="section-head">
                <h2>⚡ İşlem Takip Merkezi</h2>
                <button class="btn btn-primary" onclick="core.openIslemModal()">+ Yeni İşlem Ekle</button>
            </div>
            <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;margin-bottom:1.5rem;" id="islem-stats"></div>
            <div class="tab-btns">
                <button class="islem-page-tab tab-btn active" onclick="core.filterIslemPage('tum',this)">📋 Tümü</button>
                <button class="islem-page-tab tab-btn" onclick="core.filterIslemPage('bekleyen',this)">⏳ Bekleyen</button>
                <button class="islem-page-tab tab-btn" onclick="core.filterIslemPage('tamamlandi',this)">✅ Tamamlandı</button>
                <button class="islem-page-tab tab-btn" onclick="core.filterIslemPage('iptal',this)">❌ İptal</button>
            </div>
            <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;" id="islem-page-grid"></div>
        </div>

        <!-- SATIŞLAR -->
        <div id="page-satis" class="page">
            <div class="section-head">
                <h2>🛒 Satış & Fatura Kayıtları</h2>
                <div style="display:flex;gap:10px;">
                    <button class="btn btn-primary" onclick="core.openSatisModal()">+ Yeni Satış</button>
                    <button class="btn btn-secondary" onclick="core.exportPDF()">📄 PDF İndir</button>
                </div>
            </div>
            <div class="tab-btns">
                <button class="tab-btn active" onclick="core.filterSatis('tum',this)">Tümü</button>
                <button class="tab-btn" onclick="core.filterSatis('fatura',this)">Faturalar</button>
                <button class="tab-btn" onclick="core.filterSatis('irsaliye',this)">İrsaliyeler</button>
                <button class="tab-btn" onclick="core.filterSatis('nakit',this)">Nakit</button>
            </div>
            <div class="glass-panel">
                <table>
                    <thead><tr><th>Tarih</th><th>Müşteri</th><th>Ürün</th><th>Miktar</th><th>Evrak</th><th>İskonto</th><th>Net Tutar</th><th>Durum</th><th>İşlem</th></tr></thead>
                    <tbody id="satis-tbody"></tbody>
                </table>
            </div>
        </div>

        <!-- ÜRÜNLER / STOK -->
        <div id="page-urunler" class="page">
            <div class="section-head">
                <h2>📦 Stok & Envanter Yönetimi</h2>
                <div style="display:flex;gap:10px;">
                    <button class="btn btn-primary" onclick="core.openUrunModal()">+ Yeni Ürün Ekle</button>
                    <button class="btn btn-secondary" onclick="core.excelSablon()">📥 Excel Şablonu</button>
                    <label class="btn" style="background:#388E3C;color:#fff;cursor:pointer;">📤 Excel Yükle<input type="file" accept=".xlsx,.xls" style="display:none;" onchange="core.excelImport(this)"></label>
                </div>
            </div>
            <div class="cat-filter" id="urun-cat-filter">
                <button class="cat-btn active" onclick="core.filterUrun('tum',this)">Tümü</button>
                <button class="cat-btn" onclick="core.filterUrun('Kahve Çeşitleri',this)">☕ Kahve</button>
                <button class="cat-btn" onclick="core.filterUrun('Çay Çeşitleri',this)">🍵 Çay</button>
                <button class="cat-btn" onclick="core.filterUrun('Ekipmanlar',this)">⚙️ Ekipman</button>
                <button class="cat-btn" onclick="core.filterUrun('Sarf Malzeme',this)">🧴 Sarf</button>
                <button class="cat-btn" onclick="core.filterUrun('Kapsül & Paket',this)">📦 Kapsül</button>
            </div>
            <div class="glass-panel">
                <table>
                    <thead><tr><th>Ürün Adı</th><th>Kategori</th><th>Form</th><th>Stok</th><th>Min.Stok</th><th>Alış</th><th>Satış</th><th>Durum</th><th>İşlem</th></tr></thead>
                    <tbody id="urun-tbody"></tbody>
                </table>
            </div>
        </div>

        <!-- ZİYARET -->
        <div id="page-ziyaret" class="page">
            <div class="section-head">
                <h2>🗓️ Saha Ziyaret & Deadline Takibi</h2>
                <button class="btn btn-primary" onclick="core.openZiyaretModal()">+ Ziyaret Planla</button>
            </div>
            <div class="tab-btns">
                <button class="tab-btn active" onclick="core.filterZiyaret('tum',this)">Tümü</button>
                <button class="tab-btn" onclick="core.filterZiyaret('planlandi',this)">Planlandı</button>
                <button class="tab-btn" onclick="core.filterZiyaret('tamamlandi',this)">Tamamlandı</button>
                <button class="tab-btn" onclick="core.filterZiyaret('gecikti',this)">⚠️ Gecikti</button>
            </div>
            <div class="glass-panel">
                <table>
                    <thead><tr><th>Ziyaret Tarihi</th><th>Müşteri</th><th>Amaç</th><th>Gözlem Notu</th><th>Deadline</th><th>Aksiyon</th><th>Durum</th><th>İşlem</th></tr></thead>
                    <tbody id="ziyaret-tbody"></tbody>
                </table>
            </div>
        </div>

        <!-- MÜŞTERİLER -->
        <div id="page-musteriler" class="page">
            <div class="section-head">
                <h2>👥 Müşteri Yönetim Paneli</h2>
                <button class="btn btn-primary" onclick="core.openMusteriModal()">+ Yeni Müşteri</button>
            </div>
            <div style="display:flex;gap:10px;margin-bottom:1rem;">
                <input id="musteri-ara" type="text" placeholder="🔍 Müşteri ara..." style="padding:10px 16px;border:1.5px solid #eee;border-radius:12px;font-size:13px;width:300px;" oninput="core.renderMusteriler()">
                <select id="musteri-klasman-f" style="padding:10px 16px;border:1.5px solid #eee;border-radius:12px;font-size:13px;" onchange="core.renderMusteriler()">
                    <option value="">Tüm Klasmanlar</option>
                    <option>VIP</option><option>Premium</option><option>Standart</option><option>Yeni</option>
                </select>
                <select id="musteri-tur-f" style="padding:10px 16px;border:1.5px solid #eee;border-radius:12px;font-size:13px;" onchange="core.renderMusteriler()">
                    <option value="">Tüm Türler</option>
                    <option value="kurumsal">Kurumsal</option>
                    <option value="bireysel">Bireysel</option>
                </select>
            </div>
            <div class="glass-panel">
                <table>
                    <thead><tr><th>Müşteri / Unvan</th><th>Tür</th><th>Klasman</th><th>Tahmini Stok</th><th>Son Alım</th><th>Borç Durumu</th><th>İşlem</th></tr></thead>
                    <tbody id="musteri-tbody"></tbody>
                </table>
            </div>
        </div>

        <!-- KLASMAN -->
        <div id="page-klasman" class="page">
            <div class="section-head">
                <h2>🏆 Müşteri Klasmanı & Profil Analizi</h2>
                <div style="display:flex;gap:10px;">
                    <input id="klasman-ara" type="text" placeholder="🔍 Müşteri ara..." style="padding:10px 16px;border:1.5px solid #eee;border-radius:12px;font-size:13px;width:220px;" oninput="core.renderKlasman()">
                </div>
            </div>
            <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:1.2rem;margin:1rem 0;" id="klasman-stats"></div>
            <div class="cat-filter" id="klasman-cat-filter">
                <button class="klas-btn cat-btn active" onclick="core.filterKlasman('tum',this)">🏅 Tümü</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('VIP',this)">👑 VIP</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('Premium',this)">⭐ Premium</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('Standart',this)">✅ Standart</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('Yeni',this)">🌱 Yeni</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('kurumsal',this)" style="border-color:#2C5F7C;color:#2C5F7C;">🏢 Kurumsal</button>
                <button class="klas-btn cat-btn" onclick="core.filterKlasman('bireysel',this)" style="border-color:#6F4E37;color:#6F4E37;">👤 Bireysel</button>
            </div>
            <div id="klasman-grid" style="display:grid;grid-template-columns:repeat(3,1fr);gap:1.5rem;"></div>
        </div>

        <!-- CARİ -->
        <div id="page-cari" class="page">
            <div class="section-head">
                <h2>💰 Cari Hesap Takibi</h2>
                <button class="btn btn-primary" onclick="core.openCariModal()">+ Tahsilat / Ödeme Ekle</button>
            </div>
            <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:1.2rem;margin-bottom:1.5rem;" id="cari-totals"></div>
            <div class="glass-panel">
                <h3 style="margin-bottom:1rem;color:var(--primer);">Müşteri Bakiye Özeti</h3>
                <table>
                    <thead><tr><th>Müşteri</th><th>Klasman</th><th>Toplam Satış</th><th>Tahsilat</th><th>Bakiye</th><th>Son Hareket</th><th>İşlem</th></tr></thead>
                    <tbody id="cari-tbody"></tbody>
                </table>
            </div>
            <div class="glass-panel">
                <h3 style="margin-bottom:1rem;color:var(--primer);">Cari Hareket Defteri</h3>
                <table>
                    <thead><tr><th>Tarih</th><th>Müşteri</th><th>Tür</th><th>Tutar</th><th>Açıklama</th></tr></thead>
                    <tbody id="cari-harekets"></tbody>
                </table>
            </div>
        </div>

        <!-- RAPORLAR -->
        <div id="page-raporlar" class="page">
            <div class="section-head">
                <h2>📈 Finansal Analiz & Raporlama</h2>
                <div style="display:flex;gap:10px;">
                    <button class="btn btn-primary" onclick="core.exportPDFFull()">📄 Tam Rapor PDF</button>
                    <button class="btn btn-secondary" onclick="core.exportRaporExcel()">📊 Excel İndir</button>
                </div>
            </div>
            <div style="display:grid;grid-template-columns:repeat(4,1fr);gap:1.2rem;margin:1.5rem 0;" id="rapor-totals"></div>
            <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:1.5rem;margin-bottom:1.5rem;" id="rapor-financials"></div>
            <div class="glass-panel">
                <h3 style="margin-bottom:1rem;color:var(--primer);">📋 Karlılık Detay Tablosu</h3>
                <table>
                    <thead><tr><th>Tarih</th><th>Müşteri</th><th>Ürün</th><th>Evrak</th><th>İskonto</th><th>Maliyet</th><th>Satış</th><th>Net Kâr</th></tr></thead>
                    <tbody id="rapor-table"></tbody>
                </table>
            </div>
        </div>

        <!-- ADMIN PANELİ -->
        <div id="page-admin" class="page">
            <div class="section-head">
                <h2>⚙️ Admin Yönetim Paneli</h2>
                <button class="btn btn-primary" onclick="core.openUserModal()">+ Yeni Personel Ekle</button>
            </div>
            <div class="glass-panel">
                <h3 style="margin-bottom:1rem;color:var(--primer);">👤 Kullanıcı & Yetki Yönetimi</h3>
                <table>
                    <thead><tr><th>Kullanıcı</th><th>Rol</th><th>Maks. İskonto</th><th>Excel Yetkisi</th><th>Değişiklik Yetkisi</th><th>İşlem</th></tr></thead>
                    <tbody id="admin-tbody"></tbody>
                </table>
            </div>
            <div class="glass-panel" style="margin-top:1.5rem;">
                <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem;">
                    <h3 style="color:var(--primer);">🔔 Sistem Uyarı Ayarları</h3>
                    <span style="font-size:11px;color:#888;background:#f5f5f5;padding:6px 12px;border-radius:8px;">Sadece Admin değiştirebilir</span>
                </div>
                <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:20px;margin-bottom:1.5rem;">
                    <div style="background:#f8f9fa;border-radius:15px;padding:1.5rem;border:2px solid #eee;transition:0.3s;" onmouseover="this.style.borderColor='var(--primer)'" onmouseout="this.style.borderColor='#eee'">
                        <div style="font-size:24px;margin-bottom:8px;">📦</div>
                        <label style="font-size:11px;font-weight:800;color:var(--primer);text-transform:uppercase;letter-spacing:0.5px;display:block;margin-bottom:8px;">Ürün Stok Kritik Eşiği</label>
                        <p style="font-size:11px;color:#888;margin-bottom:12px;">Bu sayıda veya altında stok kaldığında uyarı verilir.</p>
                        <div style="display:flex;align-items:center;gap:10px;">
                            <input type="number" id="set-stok-esik" value="5" min="1" style="flex:1;padding:10px 14px;border:1.5px solid #ddd;border-radius:10px;font-size:14px;font-weight:700;text-align:center;">
                            <span style="font-size:13px;color:#888;">adet</span>
                        </div>
                    </div>
                    <div style="background:#f8f9fa;border-radius:15px;padding:1.5rem;border:2px solid #eee;transition:0.3s;" onmouseover="this.style.borderColor='var(--warn)'" onmouseout="this.style.borderColor='#eee'">
                        <div style="font-size:24px;margin-bottom:8px;">🏪</div>
                        <label style="font-size:11px;font-weight:800;color:var(--warn);text-transform:uppercase;letter-spacing:0.5px;display:block;margin-bottom:8px;">Müşteri Stok Uyarı Oranı</label>
                        <p style="font-size:11px;color:#888;margin-bottom:12px;">Müşteri stok tahmini bu oranın altına düştüğünde uyarı verilir.</p>
                        <div style="display:flex;align-items:center;gap:10px;">
                            <input type="number" id="set-musteri-esik" value="20" min="1" max="99" style="flex:1;padding:10px 14px;border:1.5px solid #ddd;border-radius:10px;font-size:14px;font-weight:700;text-align:center;">
                            <span style="font-size:13px;color:#888;">%</span>
                        </div>
                    </div>
                    <div style="background:#f8f9fa;border-radius:15px;padding:1.5rem;border:2px solid #eee;transition:0.3s;" onmouseover="this.style.borderColor='var(--danger)'" onmouseout="this.style.borderColor='#eee'">
                        <div style="font-size:24px;margin-bottom:8px;">⏰</div>
                        <label style="font-size:11px;font-weight:800;color:var(--danger);text-transform:uppercase;letter-spacing:0.5px;display:block;margin-bottom:8px;">Deadline Öncesi Uyarı</label>
                        <p style="font-size:11px;color:#888;margin-bottom:12px;">Son tarihten bu kadar gün önce uyarı verilmeye başlanır.</p>
                        <div style="display:flex;align-items:center;gap:10px;">
                            <input type="number" id="set-deadline-esik" value="2" min="0" style="flex:1;padding:10px 14px;border:1.5px solid #ddd;border-radius:10px;font-size:14px;font-weight:700;text-align:center;">
                            <span style="font-size:13px;color:#888;">gün önce</span>
                        </div>
                    </div>
                </div>
                <div style="display:flex;justify-content:space-between;align-items:center;">
                    <div id="ayar-msg" style="font-size:13px;font-weight:600;color:var(--success);"></div>
                    <button class="btn btn-success" style="padding:14px 30px;font-size:14px;" onclick="core.saveSettings()">💾 Ayarları Kaydet</button>
                </div>
            </div>
            <div class="glass-panel" style="margin-top:1.5rem;">
                <h3 style="margin-bottom:1rem;color:var(--primer);">📱 Admin WhatsApp & Bildirim Numarası</h3>
                <p style="font-size:13px;color:#888;margin-bottom:1rem;">Sabah uyarı mesajları ve kritik bildirimler bu numaraya gönderilir.</p>
                <div style="display:flex;gap:12px;align-items:flex-end;">
                    <div class="fg" style="flex:1;margin-bottom:0;">
                        <label>Admin WhatsApp Numarası (Ülke kodu dahil)</label>
                        <input type="text" id="admin-tel-set" placeholder="905xxxxxxxxx" style="border:1.5px solid #eee;border-radius:12px;padding:12px;font-size:15px;">
                    </div>
                    <button class="btn btn-success" onclick="core.saveAdminTelAdmin()">💾 Kaydet</button>
                    <button class="btn btn-wa" onclick="core.waAdminTest()">📱 Test Gönder</button>
                </div>
                <div id="admin-tel-msg" style="font-size:12px;margin-top:8px;font-weight:600;color:var(--success);"></div>
            </div>
                <p style="font-size:13px;color:#888;margin-bottom:1rem;">Seçilen kullanıcıyı bir sonraki girişte yeni şifre oluşturmaya zorlayabilirsiniz.</p>
                <div style="display:flex;gap:10px;align-items:center;">
                    <select id="reset-user-sel" style="padding:11px 15px;border:1.5px solid #eee;border-radius:12px;font-size:13px;flex:1;">
                        <option value="">-- Kullanıcı Seçin --</option>
                    </select>
                    <button class="btn btn-warn" onclick="core.forceReset()">🔄 Şifre Sıfırlamayı Zorla</button>
                </div>
            </div>
        </div>

    </main>
</div>

<!-- ============================
     MODALS
============================= -->

<!-- YENİ SATIŞ MODALI -->
<div class="modal-bg" id="mod-satis">
    <div class="modal">
        <h3 style="margin-bottom:2rem;color:var(--primer);">🛒 Yeni Satış Kaydı</h3>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg">
                <label>Müşteri</label>
                <select id="s-musteri" onchange="core.satisMusteri()">
                    <option value="">-- Müşteri Seçin --</option>
                </select>
            </div>
            <div class="fg">
                <label>Evrak Türü</label>
                <select id="s-evrak">
                    <option value="Fatura">Fatura</option>
                    <option value="İrsaliye">İrsaliye</option>
                    <option value="Nakit">Nakit Satış</option>
                </select>
            </div>
            <div class="fg">
                <label>Ürün</label>
                <select id="s-urun" onchange="core.satisUrun()">
                    <option value="">-- Ürün Seçin --</option>
                </select>
            </div>
            <div class="fg">
                <label>Miktar</label>
                <input type="number" id="s-miktar" value="1" min="1" oninput="core.satisHesap()">
            </div>
            <div class="fg">
                <label>Kampanya / Promosyon</label>
                <select id="s-kampanya" onchange="core.satisHesap()">
                    <option value="0">Kampanya Yok</option>
                    <option value="5">Hoş Geldin İndirimi (%5)</option>
                    <option value="10">Müdavim Müşteri (%10)</option>
                    <option value="15">Geri Dönen Müşteri (%15)</option>
                    <option value="20">Sezon Sonu Kampanyası (%20)</option>
                </select>
            </div>
            <div class="fg">
                <label>Ek İskonto % <span id="iskonto-limit-label" style="color:var(--accent);font-size:10px;"></span></label>
                <input type="number" id="s-iskonto" value="0" min="0" max="100" oninput="core.satisHesap()">
            </div>
            <div class="fg">
                <label>Birim Satış Fiyatı (TL)</label>
                <input type="number" id="s-fiyat" value="0" oninput="core.satisHesap()">
            </div>
            <div class="fg">
                <label>Ödeme Durumu</label>
                <select id="s-odeme">
                    <option value="Açık">Açık (Vadeli)</option>
                    <option value="Tahsil">Tahsil Edildi</option>
                    <option value="Kısmi">Kısmi Ödeme</option>
                </select>
            </div>
        </div>
        <div style="background:rgba(111,78,55,0.06);border-radius:15px;padding:1.5rem;margin:1rem 0;">
            <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:8px;"><span>Brüt Tutar:</span><span id="s-brut">0 TL</span></div>
            <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:8px;color:var(--warn);"><span>Kampanya İndirimi:</span><span id="s-camp-indirim">0 TL</span></div>
            <div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:8px;color:var(--danger);"><span>Ek İskonto:</span><span id="s-ek-indirim">0 TL</span></div>
            <div style="display:flex;justify-content:space-between;font-size:16px;font-weight:800;border-top:1px solid #ddd;padding-top:10px;"><span>NET TUTAR:</span><span id="s-net" style="color:var(--success);">0 TL</span></div>
        </div>
        <div class="fg">
            <label>Not / Açıklama</label>
            <textarea id="s-not" rows="2" style="resize:none;"></textarea>
        </div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveSatis()">✅ SATIŞ KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- ÜRÜN EKLEME MODALI -->
<div class="modal-bg" id="mod-urun">
    <div class="modal-sm">
        <h3 style="margin-bottom:2rem;color:var(--primer);">📦 Yeni Ürün Kaydı</h3>
        <div class="fg">
            <label>Ürün Adı</label>
            <input type="text" id="u-ad" placeholder="Ürün adını girin">
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg">
                <label>Kategori</label>
                <select id="u-kat">
                    <option>Kahve Çeşitleri</option>
                    <option>Çay Çeşitleri</option>
                    <option>Ekipmanlar</option>
                    <option>Sarf Malzeme</option>
                    <option>Kapsül & Paket</option>
                </select>
            </div>
            <div class="fg">
                <label>Form / Birim</label>
                <select id="u-form">
                    <option>Çekirdek</option>
                    <option>Öğütülmüş</option>
                    <option>Dökme</option>
                    <option>Adet</option>
                    <option>Paket</option>
                    <option>Kapsül</option>
                    <option>Kg</option>
                </select>
            </div>
            <div class="fg">
                <label>Alış Fiyatı (TL)</label>
                <input type="number" id="u-alis" value="0">
            </div>
            <div class="fg">
                <label>Satış Fiyatı (TL)</label>
                <input type="number" id="u-satis" value="0">
            </div>
            <div class="fg">
                <label>Mevcut Stok</label>
                <input type="number" id="u-stok" value="0">
            </div>
            <div class="fg">
                <label>Minimum Stok (Uyarı Eşiği)</label>
                <input type="number" id="u-min" value="5">
            </div>
        </div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveUrun()">💾 KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- MÜŞTERİ EKLEME MODALI -->
<div class="modal-bg" id="mod-musteri">
    <div class="modal">
        <h3 style="margin-bottom:2rem;color:var(--primer);">👤 Müşteri Kayıt Formu</h3>
        <div class="fg">
            <label>Müşteri Türü</label>
            <select id="m-tur" onchange="core.toggleMFields()">
                <option value="bireysel">Bireysel / Gerçek Kişi</option>
                <option value="kurumsal">Kurumsal / Tüzel Kişi</option>
            </select>
        </div>
        <div class="fg"><label id="m-ad-label">Ad Soyad</label><input type="text" id="m-ad"></div>
        <div id="m-kurumsal-fields" style="display:none;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg"><label>Tam Ticari Unvan</label><input type="text" id="m-unvan" style="grid-column:span 2;"></div>
            <div class="fg"><label>Vergi Dairesi</label><input type="text" id="m-vd"></div>
            <div class="fg"><label>Vergi No / VKN</label><input type="text" id="m-vkn"></div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg"><label>Telefon (WhatsApp)</label><input type="text" id="m-tel" placeholder="905xxxxxxxxx"></div>
            <div class="fg"><label>E-Posta</label><input type="text" id="m-mail"></div>
            <div class="fg"><label>Adres</label><input type="text" id="m-adres"></div>
            <div class="fg"><label>Şehir</label><input type="text" id="m-sehir"></div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg">
                <label>Müşteri Klasmanı</label>
                <select id="m-klasman">
                    <option>Yeni</option>
                    <option>Standart</option>
                    <option>Premium</option>
                    <option>VIP</option>
                </select>
            </div>
            <div class="fg"><label>Tahmini Aylık Tüketim (Kg)</label><input type="number" id="m-tuketim" value="10"></div>
        </div>
        <div class="fg"><label>Notlar</label><textarea id="m-not" rows="2" style="resize:none;"></textarea></div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveMusteri()">💾 KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- ZİYARET MODALI -->
<div class="modal-bg" id="mod-ziyaret">
    <div class="modal-sm">
        <h3 style="margin-bottom:2rem;color:var(--primer);">🗓️ Ziyaret Planla / Kaydet</h3>
        <div class="fg">
            <label>Müşteri</label>
            <select id="z-musteri"><option value="">-- Müşteri Seçin --</option></select>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg"><label>Ziyaret Tarihi</label><input type="date" id="z-tarih"></div>
            <div class="fg"><label>Deadline (Son Tarih)</label><input type="date" id="z-deadline"></div>
        </div>
        <div class="fg">
            <label>Ziyaret Amacı</label>
            <select id="z-amac">
                <option>Rutin Ziyaret</option>
                <option>Teklif Sunumu</option>
                <option>Numune Teslimi</option>
                <option>Şikayet Takibi</option>
                <option>Sipariş Alma</option>
                <option>Makine Bakımı</option>
                <option>Tahsilat</option>
            </select>
        </div>
        <div class="fg"><label>Gözlem Notu (Neler Görüldü?)</label><textarea id="z-gozlem" rows="3" style="resize:none;" placeholder="Müşterinin makine durumu, rakip ürün kullanımı, stok görünümü..."></textarea></div>
        <div class="fg">
            <label>Aksiyon (Deadline için)</label>
            <input type="text" id="z-aksiyon" placeholder="Örn: Teklif gönderilecek, numune hazırlanacak">
        </div>
        <div class="fg">
            <label>Durum</label>
            <select id="z-durum">
                <option value="planlandi">Planlandı</option>
                <option value="tamamlandi">Tamamlandı</option>
            </select>
        </div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveZiyaret()">💾 KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- MÜŞTERİ GÖZ ANALİZ MODALI -->
<div class="modal-bg" id="mod-goz">
    <div class="modal-lg">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1.5rem;border-bottom:2px solid #eee;padding-bottom:15px;">
            <h2 id="goz-baslik" style="color:var(--primer);">Müşteri Analiz</h2>
            <div style="display:flex;gap:10px;" id="goz-actions"></div>
        </div>
        <div id="goz-icerik"></div>
        <button class="btn btn-danger" style="margin-top:2rem;" onclick="core.closeM()">KAPAT</button>
    </div>
</div>

<!-- PERSONEL EKLEME MODALI -->
<div class="modal-bg" id="mod-user">
    <div class="modal-sm">
        <h3 style="margin-bottom:2rem;color:var(--primer);">👤 Personel Kaydı & Yetki</h3>
        <div class="fg"><label>Kullanıcı Adı</label><input type="text" id="u-user"></div>
        <div class="fg"><label>Şifre</label><input type="password" id="u-pass"></div>
        <div class="fg">
            <label>Rol</label>
            <select id="u-role">
                <option value="personel">Personel</option>
                <option value="mudur">Müdür</option>
                <option value="admin">Admin</option>
            </select>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg"><label>Maks. İskonto %</label><input type="number" id="u-max" value="10" min="0" max="100"></div>
            <div class="fg"><label>Excel Yetkisi</label>
                <select id="u-excel"><option value="true">Var</option><option value="false">Yok</option></select>
            </div>
        </div>
        <div class="fg"><label>Değişiklik / Silme Yetkisi</label>
            <select id="u-change"><option value="true">Var</option><option value="false">Yok</option></select>
        </div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveUser()">💾 KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- CARİ HAREKET MODALI -->
<div class="modal-bg" id="mod-cari">
    <div class="modal-sm">
        <h3 style="margin-bottom:2rem;color:var(--primer);">💰 Cari Hareket Ekle</h3>
        <div class="fg">
            <label>Müşteri</label>
            <select id="c-musteri"><option value="">-- Müşteri Seçin --</option></select>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg">
                <label>Hareket Türü</label>
                <select id="c-tur">
                    <option value="tahsilat">Tahsilat (Ödeme Aldık)</option>
                    <option value="odeme">Ödeme (Müşteriye Verdik)</option>
                    <option value="duzeltme">Düzeltme</option>
                </select>
            </div>
            <div class="fg"><label>Tutar (TL)</label><input type="number" id="c-tutar" value="0"></div>
            <div class="fg"><label>Tarih</label><input type="date" id="c-tarih"></div>
            <div class="fg">
                <label>Ödeme Şekli</label>
                <select id="c-sekil">
                    <option>Nakit</option>
                    <option>Banka Havalesi</option>
                    <option>Çek</option>
                    <option>Senet</option>
                    <option>Kredi Kartı</option>
                </select>
            </div>
        </div>
        <div class="fg"><label>Açıklama</label><input type="text" id="c-aciklama"></div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-primary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveCari()">💾 KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- ===========================
     JAVASCRIPT MOTORU
=========================== -->
<script>
const core = {
    user: null,
    db: {
        users: [{user:'admin', pass:'1234', role:'admin', max:100, excel:true, change:true, firstLogin:true}],
        products: [], sales: [], customers: [], visits: [], cariHarekets: [],
        islemler: [],
        settings: {stokEsik:5, musteriEsik:20, deadlineEsik:2},
        dismissedAlerts: [],
        adminTel: null,
        lastAlertDate: null
    },

    // ===================== INIT =====================
    init: () => {
        const d = localStorage.getItem('erp_ultra_premium_v3');
        if(d) core.db = JSON.parse(d);
        if(!core.db.settings) core.db.settings = {stokEsik:5, musteriEsik:20, deadlineEsik:2};
        if(!core.db.cariHarekets) core.db.cariHarekets = [];
        if(!core.db.islemler) core.db.islemler = [];
        if(!core.db.dismissedAlerts) core.db.dismissedAlerts = [];
        if(core.db.adminTel === undefined) core.db.adminTel = null;
        if(core.db.lastAlertDate === undefined) core.db.lastAlertDate = null;
        if(core.db.products.length === 0) core.loadDemoData();
        else core.render();
    },

    save: () => {
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        core.render();
    },

    loadDemoData: () => {
        core.db.products = [
            {id:1, ad:'Ethiopia Sidamo G1', kat:'Kahve Çeşitleri', form:'Çekirdek', alis:280, satis:550, stok:45, min:10},
            {id:2, ad:'Colombia Huila Supremo', kat:'Kahve Çeşitleri', form:'Çekirdek', alis:260, satis:520, stok:8, min:10},
            {id:3, ad:'Guatemala Antigua', kat:'Kahve Çeşitleri', form:'Çekirdek', alis:270, satis:540, stok:22, min:8},
            {id:4, ad:'Brazil Santos Natural', kat:'Kahve Çeşitleri', form:'Öğütülmüş', alis:220, satis:430, stok:30, min:10},
            {id:5, ad:'Yemen Mocha', kat:'Kahve Çeşitleri', form:'Çekirdek', alis:450, satis:900, stok:12, min:5},
            {id:6, ad:'Kenya AA Top', kat:'Kahve Çeşitleri', form:'Çekirdek', alis:310, satis:620, stok:18, min:5},
            {id:7, ad:'Earl Grey Imperial', kat:'Çay Çeşitleri', form:'Dökme', alis:110, satis:240, stok:20, min:5},
            {id:8, ad:'Rize Turist Çayı', kat:'Çay Çeşitleri', form:'Dökme', alis:80, satis:160, stok:35, min:10},
            {id:9, ad:'Yeşil Çay Premium', kat:'Çay Çeşitleri', form:'Paket', alis:90, satis:180, stok:4, min:8},
            {id:10, ad:'Hario V60 Set', kat:'Ekipmanlar', form:'Adet', alis:450, satis:950, stok:5, min:2},
            {id:11, ad:'Chemex 6 Cup', kat:'Ekipmanlar', form:'Adet', alis:680, satis:1400, stok:3, min:2},
            {id:12, ad:'Baratza Encore Öğütücü', kat:'Ekipmanlar', form:'Adet', alis:1800, satis:3800, stok:2, min:1},
            {id:13, ad:'Nespresso Kapsül Intenso', kat:'Kapsül & Paket', form:'Kapsül', alis:15, satis:28, stok:200, min:50},
            {id:14, ad:'Filtre Kağıt 100lü', kat:'Sarf Malzeme', form:'Paket', alis:25, satis:55, stok:60, min:20},
        ];
        core.db.customers = [
            {id:1, tur:'kurumsal', ad:'Mehmet Kara', unvan:'Premium Coffee & Roasting A.Ş.', vd:'Mecidiyeköy VD', vkn:'4440001122', tel:'905320001122', mail:'mehmet@premiumcoffee.com', adres:'Levent Mah.', sehir:'İstanbul', klasman:'VIP', tuketim:40, sonSatis:'2025-12-15', not:'Çok değerli müşteri, aylık büyük sipariş verir.'},
            {id:2, tur:'kurumsal', ad:'Selin Arslan', unvan:'Cafe Strada İşletmeciliği Ltd.', vd:'Kadıköy VD', vkn:'3330001155', tel:'905335556677', mail:'selin@cafestrada.com', adres:'Moda Cad. 45', sehir:'İstanbul', klasman:'Premium', tuketim:25, sonSatis:'2025-12-20', not:'Aylık düzenli sipariş.'},
            {id:3, tur:'bireysel', ad:'Ahmet Yılmaz', unvan:'', vd:'', vkn:'', tel:'905051112233', mail:'ahmet@mail.com', adres:'', sehir:'Ankara', klasman:'Standart', tuketim:5, sonSatis:'2025-11-10', not:''},
            {id:4, tur:'kurumsal', ad:'Fatma Demir', unvan:'Demir Otel & Restaurant', vd:'Antalya VD', vkn:'5550001199', tel:'905422223344', mail:'fatma@demirotel.com', adres:'Lara Yolu', sehir:'Antalya', klasman:'VIP', tuketim:60, sonSatis:'2025-12-01', not:'Otel zinciri, çok yüksek hacim.'},
            {id:5, tur:'bireysel', ad:'Can Öztürk', unvan:'', vd:'', vkn:'', tel:'905301234567', mail:'can@mail.com', adres:'', sehir:'İzmir', klasman:'Yeni', tuketim:3, sonSatis:'2025-12-22', not:''},
        ];
        const now = new Date();
        const fmt = d => d.toISOString().split('T')[0];
        core.db.sales = [
            {id:1, mId:1, mAd:'Premium Coffee A.Ş.', uId:1, uAd:'Ethiopia Sidamo G1', miktar:10, alisF:280, satisFiyat:550, evrak:'Fatura', kampanya:5, iskonto:0, tutar:5225, odeme:'Tahsil', tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-15)), not:''},
            {id:2, mId:2, mAd:'Cafe Strada Ltd.', uId:3, uAd:'Guatemala Antigua', miktar:5, alisF:270, satisFiyat:540, evrak:'Fatura', kampanya:0, iskonto:0, tutar:2700, odeme:'Açık', tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-10)), not:''},
            {id:3, mId:4, mAd:'Demir Otel', uId:5, uAd:'Yemen Mocha', miktar:8, alisF:450, satisFiyat:900, evrak:'İrsaliye', kampanya:10, iskonto:5, tutar:6120, odeme:'Kısmi', tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-5)), not:''},
            {id:4, mId:3, mAd:'Ahmet Yılmaz', uId:7, uAd:'Earl Grey Imperial', miktar:2, alisF:110, satisFiyat:240, evrak:'Nakit', kampanya:5, iskonto:0, tutar:456, odeme:'Tahsil', tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-3)), not:''},
            {id:5, mId:1, mAd:'Premium Coffee A.Ş.', uId:2, uAd:'Colombia Huila Supremo', miktar:15, alisF:260, satisFiyat:520, evrak:'Fatura', kampanya:0, iskonto:5, tutar:7410, odeme:'Açık', tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-1)), not:''},
        ];
        core.db.cariHarekets = [
            {id:1, mId:1, mAd:'Premium Coffee A.Ş.', tur:'tahsilat', tutar:5225, tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-12)), sekil:'Banka Havalesi', aciklama:'Fatura ödemesi'},
            {id:2, mId:4, mAd:'Demir Otel', tur:'tahsilat', tutar:3000, tarih:fmt(new Date(now.getFullYear(), now.getMonth(), now.getDate()-4)), sekil:'Çek', aciklama:'Kısmi ödeme'},
        ];
        const dl_gec = new Date(now); dl_gec.setDate(now.getDate()-2);
        const dl_bug = new Date(now);
        const dl_yak = new Date(now); dl_yak.setDate(now.getDate()+1);
        const dl_ilr = new Date(now); dl_ilr.setDate(now.getDate()+4);
        core.db.islemler = [
            {id:1, baslik:'Premium Coffee — 10 Kg Ethiopia Sevkiyatı', mId:1, mAd:'Premium Coffee A.Ş.', tur:'sevkiyat', oncelik:'acil', deadline:fmt(dl_gec), aciklama:'Acil sipariş, kargo bekliyor.', durum:'bekleyen', olusturma:fmt(now)},
            {id:2, baslik:'Demir Otel — 20 Kg Yemen Mocha Talebi', mId:4, mAd:'Demir Otel', tur:'siparis', oncelik:'yuksek', deadline:fmt(dl_bug), aciklama:'Otel mutfağı için acil.', durum:'bekleyen', olusturma:fmt(now)},
            {id:3, baslik:'Cafe Strada — Hario V60 Numune', mId:2, mAd:'Cafe Strada Ltd.', tur:'numune', oncelik:'normal', deadline:fmt(dl_yak), aciklama:'Numune kutusu hazırlandı, kargo edilecek.', durum:'bekleyen', olusturma:fmt(now)},
            {id:4, baslik:'Ahmet Yılmaz — Tahsilat', mId:3, mAd:'Ahmet Yılmaz', tur:'tahsilat', oncelik:'dusuk', deadline:fmt(dl_ilr), aciklama:'456 TL tahsil edilecek.', durum:'tamamlandi', olusturma:fmt(now)},
        ];
        const z1 = new Date(now); z1.setDate(now.getDate()-5);
        const z2 = new Date(now); z2.setDate(now.getDate()+3);
        const z3 = new Date(now); z3.setDate(now.getDate()-2);
        const dl1 = new Date(now); dl1.setDate(now.getDate()-1);
        const dl2 = new Date(now); dl2.setDate(now.getDate()+7);
        core.db.visits = [
            {id:1, mId:1, mAd:'Premium Coffee A.Ş.', tarih:fmt(z1), deadline:fmt(dl1), amac:'Teklif Sunumu', gozlem:'Makine eskimiş, yeni makine önerildi. Rakip ürün var depoda.', aksiyon:'Makine teklifi gönderilecek', durum:'gecikti'},
            {id:2, mId:4, mAd:'Demir Otel', tarih:fmt(now), deadline:fmt(dl2), amac:'Sipariş Alma', gozlem:'Stok düşük, 15 kg acil sipariş istedi.', aksiyon:'Sipariş işlenecek', durum:'tamamlandi'},
            {id:3, mId:2, mAd:'Cafe Strada Ltd.', tarih:fmt(z2), deadline:fmt(dl2), amac:'Rutin Ziyaret', gozlem:'Planlandı henüz gerçekleşmedi.', aksiyon:'Numune götürülecek', durum:'planlandi'},
        ];
        core.save();
    },

    // ===================== LOGIN =====================
    login: () => {
        const u = document.getElementById('log-u').value.trim();
        const p = document.getElementById('log-p').value;
        const found = core.db.users.find(x => x.user === u && x.pass === p);
        if(found) {
            core.user = found;
            document.getElementById('login-screen').style.display='none';
            // İlk giriş veya sıfırlama zorlaması kontrolü
            if(found.mustChangePass || found.firstLogin !== false) {
                core._pendingUser = found;
                const aciklama = document.getElementById('sifre-aciklama');
                if(found.mustChangePass) {
                    aciklama.innerText = `Admin tarafından şifre sıfırlama uygulandı. Lütfen yeni şifrenizi belirleyin.`;
                } else {
                    aciklama.innerText = `Hoş geldiniz, ${found.user}! İlk girişiniz tespit edildi. Güvenliğiniz için yeni bir şifre belirleyin.`;
                }
                document.getElementById('sifre-screen').style.display='flex';
                return;
            }
            core._finishLogin(found);
        } else {
            const box = document.querySelector('.login-box');
            box.style.animation='none'; void box.offsetWidth;
            box.style.animation='shake 0.4s ease';
            alert('❌ Kullanıcı adı veya şifre hatalı!');
        }
    },

    _finishLogin: (found) => {
        document.getElementById('nav-adm').style.display = found.role==='admin'?'flex':'none';
        document.getElementById('userBadge').innerText = `${found.role==='admin'?'⚙️ ':'👤 '}${found.user.toUpperCase()} (${found.role})`;
        // Admin ilk girişte telefon numarası zorunlu
        if(found.role==='admin' && !core.db.adminTel) {
            document.getElementById('mod-admin-tel').style.display='flex';
        }
        core.render();
        // Sabah uyarı kontrolü
        core.morningAlertCheck();
    },

    sifreKontrol: () => {
        const yeni = document.getElementById('sifre-yeni').value;
        const tekrar = document.getElementById('sifre-tekrar').value;
        const msg = document.getElementById('sifre-msg');
        const btn = document.getElementById('sifre-btn');
        const eski = core._pendingUser?.pass||'';
        if(!yeni) { msg.innerText=''; btn.disabled=true; btn.style.opacity='0.5'; return; }
        if(yeni.length < 6) { msg.innerText='❌ En az 6 karakter gerekli'; msg.style.color='red'; btn.disabled=true; btn.style.opacity='0.5'; return; }
        if(!/\d/.test(yeni)) { msg.innerText='❌ En az 1 rakam içermeli'; msg.style.color='red'; btn.disabled=true; btn.style.opacity='0.5'; return; }
        if(yeni === eski) { msg.innerText='❌ Eski şifrenizden farklı olmalı'; msg.style.color='red'; btn.disabled=true; btn.style.opacity='0.5'; return; }
        if(tekrar && yeni !== tekrar) { msg.innerText='❌ Şifreler eşleşmiyor'; msg.style.color='red'; btn.disabled=true; btn.style.opacity='0.5'; return; }
        if(tekrar && yeni === tekrar) { msg.innerText='✅ Şifreler eşleşiyor'; msg.style.color='green'; btn.disabled=false; btn.style.opacity='1'; return; }
        msg.innerText='⏳ Tekrar şifre giriniz'; msg.style.color='#888'; btn.disabled=true; btn.style.opacity='0.5';
    },

    sifreKaydet: () => {
        const yeni = document.getElementById('sifre-yeni').value;
        if(!core._pendingUser) return;
        const idx = core.db.users.findIndex(u=>u.user===core._pendingUser.user);
        if(idx>-1) {
            core.db.users[idx].pass = yeni;
            core.db.users[idx].firstLogin = false;
            core.db.users[idx].mustChangePass = false;
            core.user = core.db.users[idx];
            core._pendingUser = null;
        }
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        document.getElementById('sifre-screen').style.display='none';
        document.getElementById('sifre-yeni').value='';
        document.getElementById('sifre-tekrar').value='';
        document.getElementById('sifre-msg').innerText='';
        core._finishLogin(core.user);
    },

    forceReset: () => {
        const sel = document.getElementById('reset-user-sel').value;
        if(!sel) { alert('Kullanıcı seçin!'); return; }
        if(sel==='admin') { alert('Admin şifresi bu şekilde sıfırlanamaz!'); return; }
        const idx = core.db.users.findIndex(u=>u.user===sel);
        if(idx>-1) {
            core.db.users[idx].mustChangePass = true;
            core.save();
            alert(`✅ ${sel} kullanıcısı bir sonraki girişte şifre değiştirmeye zorlanacak.`);
        }
    },

    logout: () => { if(confirm('Sistemden çıkmak istiyor musunuz?')) location.reload(); },

    // ===================== NAV =====================
    nav: (id) => {
        document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
        document.getElementById('page-'+id).classList.add('active');
        document.querySelectorAll('.sidebar nav a').forEach(a => a.classList.remove('active'));
        document.querySelectorAll('.sidebar nav a').forEach(a => {
            if(a.getAttribute('onclick') && a.getAttribute('onclick').includes("'"+id+"'")) a.classList.add('active');
        });
        core.renderPage(id);
    },

    renderPage: (id) => {
        if(id==='musteriler') core.renderMusteriler();
        if(id==='klasman') core.renderKlasman();
        if(id==='cari') core.renderCari();
        if(id==='raporlar') core.renderRaporlar();
        if(id==='urunler') core.renderUrunler('tum');
        if(id==='satis') core.renderSatis('tum');
        if(id==='ziyaret') core.renderZiyaret('tum');
        if(id==='admin') core.renderAdmin();
        if(id==='islemler') core.renderIslemlerPage('tum');
    },

    // ===================== SALES =====================
    openSatisModal: () => {
        const sel = document.getElementById('s-musteri');
        sel.innerHTML = '<option value="">-- Müşteri Seçin --</option>';
        core.db.customers.forEach(m => sel.innerHTML += `<option value="${m.id}">${m.ad}${m.unvan?' - '+m.unvan:''}</option>`);
        const usel = document.getElementById('s-urun');
        usel.innerHTML = '<option value="">-- Ürün Seçin --</option>';
        core.db.products.forEach(p => usel.innerHTML += `<option value="${p.id}" data-satis="${p.satis}" data-alis="${p.alis}">${p.ad} (${p.kat}) - ${p.stok} ${p.form}</option>`);
        const maxL = document.getElementById('iskonto-limit-label');
        if(core.user) maxL.innerText = `(Maks: %${core.user.max})`;
        document.getElementById('s-iskonto').max = core.user ? core.user.max : 100;
        core.satisHesap();
        document.getElementById('mod-satis').style.display='flex';
    },

    satisMusteri: () => {}, satisUrun: () => {
        const opt = document.getElementById('s-urun').selectedOptions[0];
        if(opt && opt.dataset.satis) {
            document.getElementById('s-fiyat').value = opt.dataset.satis;
            core.satisHesap();
        }
    },

    satisHesap: () => {
        const fiyat = parseFloat(document.getElementById('s-fiyat').value)||0;
        const miktar = parseFloat(document.getElementById('s-miktar').value)||0;
        const kamp = parseFloat(document.getElementById('s-kampanya').value)||0;
        const iskonto = parseFloat(document.getElementById('s-iskonto').value)||0;
        const brut = fiyat * miktar;
        const campIndirim = brut * (kamp/100);
        const sonraki = brut - campIndirim;
        const ekIndirim = sonraki * (iskonto/100);
        const net = sonraki - ekIndirim;
        document.getElementById('s-brut').innerText = brut.toLocaleString('tr-TR') + ' TL';
        document.getElementById('s-camp-indirim').innerText = '-' + campIndirim.toLocaleString('tr-TR') + ' TL';
        document.getElementById('s-ek-indirim').innerText = '-' + ekIndirim.toLocaleString('tr-TR') + ' TL';
        document.getElementById('s-net').innerText = net.toLocaleString('tr-TR') + ' TL';
    },

    saveSatis: () => {
        const mId = parseInt(document.getElementById('s-musteri').value);
        const uId = parseInt(document.getElementById('s-urun').value);
        if(!mId||!uId) { alert('Müşteri ve ürün seçin!'); return; }
        const m = core.db.customers.find(x=>x.id===mId);
        const u = core.db.products.find(x=>x.id===uId);
        const miktar = parseFloat(document.getElementById('s-miktar').value)||1;
        const fiyat = parseFloat(document.getElementById('s-fiyat').value)||u.satis;
        const kamp = parseFloat(document.getElementById('s-kampanya').value)||0;
        let iskonto = parseFloat(document.getElementById('s-iskonto').value)||0;
        if(core.user && iskonto > core.user.max) { iskonto = core.user.max; alert(`İskonto limitiniz %${core.user.max}. Otomatik düzeltildi.`); }
        const brut = fiyat * miktar;
        const net = brut * (1-kamp/100) * (1-iskonto/100);
        if(u.stok < miktar) { alert('Yetersiz stok!'); return; }
        u.stok -= miktar;
        m.sonSatis = new Date().toISOString().split('T')[0];
        const sale = {
            id: Date.now(), mId, mAd: m.ad+(m.unvan?' / '+m.unvan:''), uId, uAd: u.ad,
            miktar, alisF: u.alis, satisFiyat: fiyat, evrak: document.getElementById('s-evrak').value,
            kampanya: kamp, iskonto: iskonto, tutar: Math.round(net),
            odeme: document.getElementById('s-odeme').value,
            tarih: new Date().toISOString().split('T')[0], not: document.getElementById('s-not').value
        };
        core.db.sales.unshift(sale);
        core.save(); core.closeM();
    },

    filterSatis: (f, btn) => {
        document.querySelectorAll('#page-satis .tab-btn').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core.renderSatis(f);
    },

    renderSatis: (filter='tum') => {
        let data = [...core.db.sales];
        if(filter!=='tum') data = data.filter(s=>s.evrak.toLowerCase()===filter||s.evrak===filter);
        document.getElementById('satis-tbody').innerHTML = data.map(s => {
            const mar = s.tutar - (s.alisF * s.miktar);
            return `<tr>
                <td>${s.tarih}</td>
                <td><b>${s.mAd}</b></td>
                <td>${s.uAd}</td>
                <td>${s.miktar}</td>
                <td><span class="badge badge-${s.evrak==='Fatura'?'fatura':s.evrak==='İrsaliye'?'irsaliye':'evrak'}">${s.evrak}</span></td>
                <td>${(s.kampanya+s.iskonto)>0?`%${s.kampanya+s.iskonto}`:'—'}</td>
                <td><b>${s.tutar.toLocaleString('tr-TR')} TL</b></td>
                <td><span style="color:${s.odeme==='Tahsil'?'green':s.odeme==='Açık'?'red':'orange'};font-weight:600;font-size:11px;">${s.odeme}</span></td>
                <td style="display:flex;gap:5px;">
                    ${core.user&&core.user.change?`<button class="btn btn-danger btn-sm" onclick="core.delSatis(${s.id})">Sil</button>`:''}
                </td>
            </tr>`;
        }).join('');
    },

    delSatis: (id) => { if(!core.user?.change) return; if(confirm('Satışı silmek istediğinizden emin misiniz?')) { core.db.sales = core.db.sales.filter(s=>s.id!==id); core.save(); } },

    // ===================== ÜRÜNLER =====================
    openUrunModal: () => { document.getElementById('mod-urun').style.display='flex'; },

    saveUrun: () => {
        const u = {
            id: Date.now(), ad: document.getElementById('u-ad').value,
            kat: document.getElementById('u-kat').value, form: document.getElementById('u-form').value,
            alis: parseFloat(document.getElementById('u-alis').value)||0,
            satis: parseFloat(document.getElementById('u-satis').value)||0,
            stok: parseFloat(document.getElementById('u-stok').value)||0,
            min: parseFloat(document.getElementById('u-min').value)||5,
        };
        if(!u.ad) { alert('Ürün adı zorunlu!'); return; }
        core.db.products.push(u); core.save(); core.closeM();
    },

    delUrun: (id) => { if(!core.user?.change) return; if(confirm('Ürünü silmek istiyorsunuz?')) { core.db.products = core.db.products.filter(p=>p.id!==id); core.save(); } },

    filterUrun: (cat, btn) => {
        document.querySelectorAll('.cat-btn').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core.renderUrunler(cat);
    },

    renderUrunler: (cat='tum') => {
        let data = [...core.db.products];
        if(cat!=='tum') data = data.filter(p=>p.kat===cat);
        document.getElementById('urun-tbody').innerHTML = data.map(p => {
            const stokDur = p.stok <= p.min ? 'danger' : p.stok <= p.min*2 ? 'warn' : 'success';
            const stokRenk = stokDur==='danger'?'red':stokDur==='warn'?'orange':'green';
            return `<tr>
                <td><b>${p.ad}</b></td>
                <td><small style="background:rgba(111,78,55,0.1);padding:3px 8px;border-radius:8px;">${p.kat}</small></td>
                <td>${p.form}</td>
                <td style="color:${stokRenk};font-weight:700;">${p.stok}</td>
                <td style="color:#888;">${p.min}</td>
                <td>${p.alis} TL</td>
                <td><b>${p.satis} TL</b></td>
                <td><span style="width:80px;display:inline-block;">
                    <div class="prog-bar"><div class="prog-fill" style="width:${Math.min(100,p.stok/Math.max(1,p.min*3)*100)}%;background:${stokRenk};"></div></div>
                </span></td>
                <td>${core.user?.change?`<button class="btn btn-danger btn-sm" onclick="core.delUrun(${p.id})">Sil</button>`:''}</td>
            </tr>`;
        }).join('');
    },

    excelSablon: () => {
        const wb = XLSX.utils.book_new();
        const ws = XLSX.utils.aoa_to_sheet([
            ['Ürün Adı','Kategori','Form','Alış Fiyatı','Satış Fiyatı','Stok','Min Stok'],
            ['Ethiopia Sidamo G1','Kahve Çeşitleri','Çekirdek',280,550,50,10]
        ]);
        XLSX.utils.book_append_sheet(wb, ws, 'Ürünler');
        XLSX.writeFile(wb, 'UrunSablon.xlsx');
    },

    excelImport: (input) => {
        if(!core.user?.excel) { alert('Excel yükleme yetkiniz yok!'); return; }
        const file = input.files[0]; if(!file) return;
        const reader = new FileReader();
        reader.onload = e => {
            const data = new Uint8Array(e.target.result);
            const wb = XLSX.read(data, {type:'array'});
            const ws = wb.Sheets[wb.SheetNames[0]];
            const rows = XLSX.utils.sheet_to_json(ws, {header:1});
            let added = 0;
            for(let i=1;i<rows.length;i++) {
                const r = rows[i];
                if(r[0]) {
                    core.db.products.push({id:Date.now()+i, ad:r[0]||'', kat:r[1]||'Kahve Çeşitleri', form:r[2]||'Adet', alis:parseFloat(r[3])||0, satis:parseFloat(r[4])||0, stok:parseFloat(r[5])||0, min:parseFloat(r[6])||5});
                    added++;
                }
            }
            core.save(); input.value=''; alert(`${added} ürün başarıyla eklendi!`);
        };
        reader.readAsArrayBuffer(file);
    },

    // ===================== ZİYARET =====================
    openZiyaretModal: () => {
        const sel = document.getElementById('z-musteri');
        sel.innerHTML = '<option value="">-- Müşteri Seçin --</option>';
        core.db.customers.forEach(m => sel.innerHTML += `<option value="${m.id}">${m.ad}</option>`);
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('z-tarih').value = today;
        document.getElementById('mod-ziyaret').style.display='flex';
    },

    saveZiyaret: () => {
        const mId = parseInt(document.getElementById('z-musteri').value);
        if(!mId) { alert('Müşteri seçin!'); return; }
        const m = core.db.customers.find(x=>x.id===mId);
        const v = {
            id: Date.now(), mId, mAd: m.ad,
            tarih: document.getElementById('z-tarih').value,
            deadline: document.getElementById('z-deadline').value,
            amac: document.getElementById('z-amac').value,
            gozlem: document.getElementById('z-gozlem').value,
            aksiyon: document.getElementById('z-aksiyon').value,
            durum: document.getElementById('z-durum').value
        };
        const bugun = new Date();
        const dl = new Date(v.deadline);
        if(v.deadline && dl < bugun && v.durum !== 'tamamlandi') v.durum = 'gecikti';
        core.db.visits.unshift(v); core.save(); core.closeM();
    },

    filterZiyaret: (f, btn) => {
        document.querySelectorAll('#page-ziyaret .tab-btn').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core.renderZiyaret(f);
    },

    renderZiyaret: (filter='tum') => {
        let data = [...core.db.visits];
        const today = new Date();
        data = data.map(v => {
            const dl = new Date(v.deadline);
            if(v.deadline && dl < today && v.durum==='planlandi') v.durum='gecikti';
            return v;
        });
        if(filter!=='tum') data = data.filter(v=>v.durum===filter);
        document.getElementById('ziyaret-tbody').innerHTML = data.map(v => {
            const durumRenk = v.durum==='tamamlandi'?'green':v.durum==='gecikti'?'red':'#2C5F7C';
            const durumEmoji = v.durum==='tamamlandi'?'✅':v.durum==='gecikti'?'🚨':'⏳';
            const m = core.db.customers.find(x=>x.id===v.mId);
            return `<tr>
                <td>${v.tarih}</td>
                <td><b>${v.mAd}</b></td>
                <td><small>${v.amac}</small></td>
                <td><span title="${v.gozlem}" style="max-width:200px;display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;">${v.gozlem||'—'}</span></td>
                <td style="color:${v.durum==='gecikti'?'red':'inherit'};font-weight:${v.durum==='gecikti'?'700':'400'};">${v.deadline||'—'}</td>
                <td><small>${v.aksiyon||'—'}</small></td>
                <td><span style="color:${durumRenk};font-weight:700;">${durumEmoji} ${v.durum.toUpperCase()}</span></td>
                <td style="display:flex;gap:5px;">
                    ${m?`<button class="btn btn-wa btn-sm" onclick="core.waZiyaret(${m.id})">📱</button>`:''}
                    <button class="btn btn-danger btn-sm" onclick="core.delZiyaret(${v.id})">Sil</button>
                </td>
            </tr>`;
        }).join('');
    },

    delZiyaret: (id) => { if(confirm('Ziyareti sil?')) { core.db.visits = core.db.visits.filter(v=>v.id!==id); core.save(); } },

    // ===================== MÜŞTERİLER =====================
    openMusteriModal: () => { core.toggleMFields(); document.getElementById('mod-musteri').style.display='flex'; },

    toggleMFields: () => {
        const isK = document.getElementById('m-tur').value === 'kurumsal';
        document.getElementById('m-kurumsal-fields').style.display = isK ? 'grid' : 'none';
        document.getElementById('m-ad-label').innerText = isK ? 'Yetkili Ad Soyad' : 'Ad Soyad';
    },

    saveMusteri: () => {
        const m = {
            id: Date.now(), tur: document.getElementById('m-tur').value,
            ad: document.getElementById('m-ad').value, unvan: document.getElementById('m-unvan').value,
            vd: document.getElementById('m-vd').value, vkn: document.getElementById('m-vkn').value,
            tel: document.getElementById('m-tel').value, mail: document.getElementById('m-mail').value,
            adres: document.getElementById('m-adres').value, sehir: document.getElementById('m-sehir').value,
            klasman: document.getElementById('m-klasman').value,
            tuketim: parseFloat(document.getElementById('m-tuketim').value)||10,
            not: document.getElementById('m-not').value, sonSatis: null
        };
        if(!m.ad) { alert('Ad zorunlu!'); return; }
        core.db.customers.push(m); core.save(); core.closeM();
    },

    delMusteri: (id) => { if(!core.user?.change) return; if(confirm('Müşteriyi sil?')) { core.db.customers = core.db.customers.filter(m=>m.id!==id); core.save(); } },

    renderMusteriler: () => {
        const ara = (document.getElementById('musteri-ara')?.value||'').toLowerCase();
        const kf = document.getElementById('musteri-klasman-f')?.value||'';
        const tf = document.getElementById('musteri-tur-f')?.value||'';
        let data = core.db.customers.filter(m => {
            const match = m.ad.toLowerCase().includes(ara)||(m.unvan||'').toLowerCase().includes(ara);
            const km = kf===''||m.klasman===kf;
            const tm = tf===''||m.tur===tf;
            return match&&km&&tm;
        });
        document.getElementById('musteri-tbody').innerHTML = data.map(m => {
            const bugun = new Date();
            const sonS = new Date(m.sonSatis||bugun);
            const gunF = Math.max(0, Math.floor((bugun-sonS)/(1000*60*60*24)));
            const kalan = Math.max(0, m.tuketim - (gunF*(m.tuketim/30))).toFixed(1);
            const bakiye = core.musteriBalans(m.id);
            return `<tr>
                <td><b>${m.ad}</b>${m.unvan?`<br><small style="color:#888;">${m.unvan}</small>`:''}</td>
                <td><span style="font-size:11px;font-weight:600;text-transform:uppercase;color:${m.tur==='kurumsal'?'#2C5F7C':'#6F4E37'};">${m.tur}</span></td>
                <td><span class="badge badge-${m.klasman.toLowerCase()==='vip'?'vip':m.klasman.toLowerCase()==='premium'?'premium':'standart'}">${m.klasman}</span></td>
                <td><b style="color:${parseFloat(kalan)<5?'red':parseFloat(kalan)<m.tuketim*0.3?'orange':'green'};">${kalan} Kg</b></td>
                <td><small>${m.sonSatis||'Hiç alım yok'}</small></td>
                <td class="${bakiye>0?'cari-neg':bakiye<0?'cari-pos':'cari-zero'}">${Math.abs(bakiye).toLocaleString('tr-TR')} TL ${bakiye>0?'BORÇ':bakiye<0?'ALACAK':'Sıfır'}</td>
                <td style="display:flex;gap:5px;flex-wrap:wrap;">
                    <button class="btn btn-sm" style="background:#eee;" onclick="core.openGoz(${m.id})">👁️</button>
                    <button class="btn btn-wa btn-sm" onclick="core.waMusteri(${m.id})">📱</button>
                    ${core.user?.change?`<button class="btn btn-danger btn-sm" onclick="core.delMusteri(${m.id})">Sil</button>`:''}
                </td>
            </tr>`;
        }).join('');
    },

    // ===================== GÖZ ANALİZ =====================
    openGoz: (mId) => {
        const m = core.db.customers.find(x=>x.id===mId);
        const mSales = core.db.sales.filter(s=>s.mId===mId);
        const toplamCiro = mSales.reduce((a,b)=>a+b.tutar, 0);
        const mVisits = core.db.visits.filter(v=>v.mId===mId);
        const bugun = new Date();
        const sonS = new Date(m.sonSatis||bugun);
        const gunF = Math.max(0, Math.floor((bugun-sonS)/(1000*60*60*24)));
        const kalan = Math.max(0, m.tuketim-(gunF*(m.tuketim/30))).toFixed(1);
        const oran = Math.min(100, (kalan/m.tuketim)*100);
        const bakiye = core.musteriBalans(mId);
        document.getElementById('goz-baslik').innerText = `${m.ad} — Anlık Analiz Raporu`;
        document.getElementById('goz-actions').innerHTML = `
            <button class="btn btn-wa btn-sm" onclick="core.waMusteri(${mId})">📱 WhatsApp</button>
        `;
        document.getElementById('goz-icerik').innerHTML = `
            <div style="display:grid;grid-template-columns:1.5fr 1fr;gap:20px;margin-bottom:20px;">
                <div style="background:#f8f9fa;padding:20px;border-radius:15px;">
                    <h4 style="margin-bottom:12px;color:var(--primer);">📋 Kart Bilgileri</h4>
                    <table style="font-size:13px;">
                        <tr><td style="padding:4px 8px 4px 0;color:#888;width:120px;">Tür:</td><td><b>${m.tur.toUpperCase()}</b></td></tr>
                        <tr><td style="padding:4px 8px 4px 0;color:#888;">Klasman:</td><td><span class="badge badge-${m.klasman.toLowerCase()==='vip'?'vip':m.klasman.toLowerCase()==='premium'?'premium':'standart'}">${m.klasman}</span></td></tr>
                        ${m.tur==='kurumsal'?`<tr><td style="padding:4px 8px 4px 0;color:#888;">Unvan:</td><td>${m.unvan||'—'}</td></tr><tr><td style="padding:4px 8px 4px 0;color:#888;">VKN/VD:</td><td>${m.vkn||'—'} / ${m.vd||'—'}</td></tr>`:''}
                        <tr><td style="padding:4px 8px 4px 0;color:#888;">Telefon:</td><td>${m.tel||'—'}</td></tr>
                        <tr><td style="padding:4px 8px 4px 0;color:#888;">E-Posta:</td><td>${m.mail||'—'}</td></tr>
                        <tr><td style="padding:4px 8px 4px 0;color:#888;">Şehir:</td><td>${m.sehir||'—'}</td></tr>
                        <tr><td style="padding:4px 8px 4px 0;color:#888;">Son Alım:</td><td>${m.sonSatis||'Yok'}</td></tr>
                    </table>
                </div>
                <div style="display:flex;flex-direction:column;gap:12px;">
                    <div style="background:rgba(111,78,55,0.08);padding:18px;border-radius:15px;text-align:center;">
                        <div style="font-size:11px;font-weight:700;color:#888;text-transform:uppercase;margin-bottom:5px;">Tahmini Kalan Stok</div>
                        <div style="font-size:36px;font-weight:800;color:${oran<20?'red':oran<50?'orange':'var(--primer)'};">${kalan}<small style="font-size:16px;"> Kg</small></div>
                        <div class="prog-bar" style="margin-top:8px;"><div class="prog-fill" style="width:${oran}%;background:${oran<20?'red':oran<50?'orange':'green'};"></div></div>
                        <div style="font-size:11px;color:#888;margin-top:5px;">Aylık tüketim: ${m.tuketim} Kg</div>
                    </div>
                    <div style="background:rgba(44,95,124,0.08);padding:18px;border-radius:15px;text-align:center;">
                        <div style="font-size:11px;font-weight:700;color:#888;text-transform:uppercase;margin-bottom:5px;">Bakiye Durumu</div>
                        <div style="font-size:20px;font-weight:800;color:${bakiye>0?'red':bakiye<0?'green':'gray'};">
                            ${Math.abs(bakiye).toLocaleString('tr-TR')} TL
                        </div>
                        <div style="font-size:11px;color:#888;">${bakiye>0?'❌ BORÇLU':bakiye<0?'✅ ALACAKLI':'✅ Dengede'}</div>
                    </div>
                    <div style="background:rgba(46,125,50,0.08);padding:18px;border-radius:15px;text-align:center;">
                        <div style="font-size:11px;font-weight:700;color:#888;text-transform:uppercase;margin-bottom:5px;">Toplam Ciro</div>
                        <div style="font-size:20px;font-weight:800;color:var(--success);">${toplamCiro.toLocaleString('tr-TR')} TL</div>
                        <div style="font-size:11px;color:#888;">${mSales.length} satış işlemi</div>
                    </div>
                </div>
            </div>
            <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
                <div>
                    <h4 style="margin-bottom:10px;color:var(--primer);">📈 Son Satışlar</h4>
                    <div style="max-height:180px;overflow-y:auto;border-radius:12px;border:1px solid #eee;">
                        <table><thead><tr><th>Tarih</th><th>Ürün</th><th>Tutar</th></tr></thead>
                        <tbody>${mSales.slice(0,8).map(s=>`<tr><td>${s.tarih}</td><td>${s.uAd}</td><td><b>${s.tutar.toLocaleString()} TL</b></td></tr>`).join('')||'<tr><td colspan="3" style="text-align:center;color:#888;">Satış yok</td></tr>'}</tbody></table>
                    </div>
                </div>
                <div>
                    <h4 style="margin-bottom:10px;color:var(--primer);">🗓️ Ziyaret Geçmişi</h4>
                    <div style="max-height:180px;overflow-y:auto;border-radius:12px;border:1px solid #eee;">
                        <table><thead><tr><th>Tarih</th><th>Amaç</th><th>Durum</th></tr></thead>
                        <tbody>${mVisits.slice(0,8).map(v=>`<tr><td>${v.tarih}</td><td>${v.amac}</td><td style="color:${v.durum==='tamamlandi'?'green':v.durum==='gecikti'?'red':'blue'};font-size:11px;font-weight:700;">${v.durum}</td></tr>`).join('')||'<tr><td colspan="3" style="text-align:center;color:#888;">Ziyaret yok</td></tr>'}</tbody></table>
                    </div>
                </div>
            </div>
            ${m.not?`<div style="background:#fffbf0;border-left:4px solid #D4855C;padding:15px;border-radius:10px;margin-top:15px;font-size:13px;color:#555;"><b>📝 Not:</b> ${m.not}</div>`:''}
        `;
        document.getElementById('mod-goz').style.display='flex';
    },

    // ===================== KLASMAN =====================
    filterKlasman: (f, btn) => {
        document.querySelectorAll('.klas-btn').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core._klasmanFilter = f;
        core.renderKlasman();
    },

    renderKlasman: () => {
        const klasmanlar = {VIP:{color:'#FFD700',icon:'👑'}, Premium:{color:'#6F4E37',icon:'⭐'}, Standart:{color:'#2C5F7C',icon:'✅'}, Yeni:{color:'#4CAF50',icon:'🌱'}};
        const stats = {};
        Object.keys(klasmanlar).forEach(k => stats[k] = core.db.customers.filter(m=>m.klasman===k));
        document.getElementById('klasman-stats').innerHTML = Object.entries(stats).map(([k,ms]) => {
            const ciro = ms.reduce((a,m)=>a+core.db.sales.filter(s=>s.mId===m.id).reduce((x,y)=>x+y.tutar,0),0);
            return `<div class="stat-card" style="border-bottom-color:${klasmanlar[k].color};cursor:pointer;" onclick="core.filterKlasman('${k}', document.querySelector('.klas-btn[onclick*=\\'${k}\\']'))">
                <small>${klasmanlar[k].icon} ${k.toUpperCase()}</small>
                <h2>${ms.length}</h2>
                <div style="font-size:11px;color:#888;margin-top:4px;">${ciro.toLocaleString('tr-TR')} TL ciro</div>
            </div>`;
        }).join('');

        const ara = (document.getElementById('klasman-ara')?.value||'').toLowerCase();
        const f = core._klasmanFilter || 'tum';
        let data = [...core.db.customers];
        if(f==='kurumsal'||f==='bireysel') data = data.filter(m=>m.tur===f);
        else if(f!=='tum') data = data.filter(m=>m.klasman===f);
        if(ara) data = data.filter(m=>(m.ad||'').toLowerCase().includes(ara)||(m.unvan||'').toLowerCase().includes(ara));
        data.sort((a,b)=>{const o={VIP:0,Premium:1,Standart:2,Yeni:3};return (o[a.klasman]||4)-(o[b.klasman]||4);});

        if(!data.length) {
            document.getElementById('klasman-grid').innerHTML = `<div style="grid-column:1/-1;text-align:center;padding:3rem;color:#aaa;font-size:15px;">Bu kategoride müşteri bulunamadı.</div>`;
            return;
        }
        document.getElementById('klasman-grid').innerHTML = data.map(m => {
            const mSales = core.db.sales.filter(s=>s.mId===m.id);
            const ciro = mSales.reduce((a,b)=>a+b.tutar, 0);
            const kl = klasmanlar[m.klasman]||{color:'#ccc',icon:'—'};
            const bakiye = core.musteriBalans(m.id);
            const bugun = new Date();
            const sonS = new Date(m.sonSatis||bugun);
            const gunF = Math.max(0,Math.floor((bugun-sonS)/(1000*60*60*24)));
            const kalan = Math.max(0,m.tuketim-(gunF*(m.tuketim/30))).toFixed(1);
            const stokOran = Math.min(100,(kalan/m.tuketim)*100);
            return `<div class="klasman-card" style="border-top:4px solid ${kl.color};">
                <div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:14px;">
                    <div>
                        <div style="font-size:16px;font-weight:800;">${kl.icon} ${m.ad}</div>
                        ${m.unvan?`<div style="font-size:11px;color:#888;margin-top:2px;">${m.unvan}</div>`:''}
                        <div style="font-size:11px;color:${m.tur==='kurumsal'?'#2C5F7C':'#6F4E37'};font-weight:600;margin-top:4px;text-transform:uppercase;">${m.tur} • ${m.sehir||'—'}</div>
                    </div>
                    <span class="badge badge-${m.klasman.toLowerCase()==='vip'?'vip':m.klasman.toLowerCase()==='premium'?'premium':'standart'}">${m.klasman}</span>
                </div>
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:12px;margin-bottom:14px;">
                    <div style="background:#f5f5f5;padding:8px 10px;border-radius:10px;"><small style="color:#888;display:block;">Ciro</small><b style="color:var(--success);">${ciro.toLocaleString('tr-TR')} TL</b></div>
                    <div style="background:#f5f5f5;padding:8px 10px;border-radius:10px;"><small style="color:#888;display:block;">Bakiye</small><b style="color:${bakiye>0?'red':bakiye<0?'green':'gray'};">${Math.abs(bakiye).toLocaleString('tr-TR')} TL ${bakiye>0?'BORÇ':bakiye<0?'ALACAK':''}</b></div>
                </div>
                <div style="margin-bottom:12px;">
                    <div style="display:flex;justify-content:space-between;font-size:11px;color:#888;margin-bottom:4px;"><span>Tahmini Müşteri Stoğu</span><b style="color:${stokOran<20?'red':stokOran<50?'orange':'green'};">${kalan} Kg</b></div>
                    <div class="prog-bar"><div class="prog-fill" style="width:${stokOran}%;background:${stokOran<20?'red':stokOran<50?'orange':'green'};"></div></div>
                </div>
                <div style="display:flex;gap:8px;">
                    <button class="btn btn-sm" style="background:#eee;flex:1;justify-content:center;" onclick="core.openGoz(${m.id})">👁️ Detay</button>
                    <button class="btn btn-wa btn-sm" onclick="core.waMusteri(${m.id})">📱</button>
                    ${core.user?.change?`<button class="btn btn-danger btn-sm" onclick="core.delMusteri(${m.id})">Sil</button>`:''}
                </div>
            </div>`;
        }).join('');
    },

    // ===================== CARİ =====================
    musteriBalans: (mId) => {
        const satisToplam = core.db.sales.filter(s=>s.mId===mId).reduce((a,b)=>a+b.tutar, 0);
        const tahsilatToplam = core.db.cariHarekets.filter(c=>c.mId===mId&&c.tur==='tahsilat').reduce((a,b)=>a+b.tutar, 0);
        const odemeToplam = core.db.cariHarekets.filter(c=>c.mId===mId&&c.tur==='odeme').reduce((a,b)=>a+b.tutar, 0);
        return satisToplam - tahsilatToplam + odemeToplam; // Pozitif = müşteri borçlu
    },

    openCariModal: () => {
        const sel = document.getElementById('c-musteri');
        sel.innerHTML = '<option value="">-- Müşteri Seçin --</option>';
        core.db.customers.forEach(m => sel.innerHTML += `<option value="${m.id}">${m.ad}</option>`);
        document.getElementById('c-tarih').value = new Date().toISOString().split('T')[0];
        document.getElementById('mod-cari').style.display='flex';
    },

    saveCari: () => {
        const mId = parseInt(document.getElementById('c-musteri').value);
        if(!mId) { alert('Müşteri seçin!'); return; }
        const m = core.db.customers.find(x=>x.id===mId);
        const h = {
            id: Date.now(), mId, mAd: m.ad,
            tur: document.getElementById('c-tur').value,
            tutar: parseFloat(document.getElementById('c-tutar').value)||0,
            tarih: document.getElementById('c-tarih').value,
            sekil: document.getElementById('c-sekil').value,
            aciklama: document.getElementById('c-aciklama').value
        };
        core.db.cariHarekets.unshift(h); core.save(); core.closeM();
    },

    renderCari: () => {
        const topSatis = core.db.sales.reduce((a,b)=>a+b.tutar, 0);
        const topTahsilat = core.db.cariHarekets.filter(c=>c.tur==='tahsilat').reduce((a,b)=>a+b.tutar, 0);
        const topAlacak = topSatis - topTahsilat;
        document.getElementById('cari-totals').innerHTML = `
            <div class="stat-card" style="border-bottom-color:#2C5F7C;"><small>TOPLAM ALACAK</small><h2 style="color:#2C5F7C;">${topAlacak.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:#2E7D32;"><small>TAHSİL EDİLEN</small><h2 style="color:#2E7D32;">${topTahsilat.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:#C62828;"><small>TAKSİT / VADELİ</small><h2 style="color:#C62828;">${core.db.sales.filter(s=>s.odeme==='Açık').reduce((a,b)=>a+b.tutar,0).toLocaleString('tr-TR')} TL</h2></div>
        `;
        document.getElementById('cari-tbody').innerHTML = core.db.customers.map(m => {
            const satis = core.db.sales.filter(s=>s.mId===m.id).reduce((a,b)=>a+b.tutar,0);
            const tah = core.db.cariHarekets.filter(c=>c.mId===m.id&&c.tur==='tahsilat').reduce((a,b)=>a+b.tutar,0);
            const bak = core.musteriBalans(m.id);
            const sonH = [...core.db.sales.filter(s=>s.mId===m.id),...core.db.cariHarekets.filter(c=>c.mId===m.id)].sort((a,b)=>new Date(b.tarih)-new Date(a.tarih))[0];
            return `<tr>
                <td><b>${m.ad}</b>${m.unvan?`<br><small style="color:#888;">${m.unvan}</small>`:''}</td>
                <td><span class="badge badge-${m.klasman.toLowerCase()==='vip'?'vip':m.klasman.toLowerCase()==='premium'?'premium':'standart'}">${m.klasman}</span></td>
                <td>${satis.toLocaleString('tr-TR')} TL</td>
                <td style="color:var(--success);">${tah.toLocaleString('tr-TR')} TL</td>
                <td class="${bak>0?'cari-neg':bak<0?'cari-pos':'cari-zero'}">${Math.abs(bak).toLocaleString('tr-TR')} TL ${bak>0?'BORÇ':bak<0?'ALACAK':''}</td>
                <td><small>${sonH?.tarih||'—'}</small></td>
                <td><button class="btn btn-primary btn-sm" onclick="core.openCariModal()">+ Hareket</button></td>
            </tr>`;
        }).join('');
        document.getElementById('cari-harekets').innerHTML = core.db.cariHarekets.map(h => `
            <tr>
                <td>${h.tarih}</td>
                <td>${h.mAd}</td>
                <td><span style="color:${h.tur==='tahsilat'?'green':'red'};font-weight:700;">${h.tur==='tahsilat'?'📥 TAHSİLAT':'📤 ÖDEME'}</span></td>
                <td><b>${h.tutar.toLocaleString('tr-TR')} TL</b></td>
                <td>${h.aciklama||'—'} <small style="color:#888;">(${h.sekil})</small></td>
            </tr>`).join('');
    },

    // ===================== RAPORLAR =====================
    renderRaporlar: () => {
        const sales = core.db.sales;
        const rev = sales.reduce((a,b)=>a+b.tutar, 0);
        const cost = sales.reduce((a,b)=>a+(b.alisF*b.miktar), 0);
        const profit = rev - cost;
        const margin = rev>0?((profit/rev)*100).toFixed(1):0;
        const basabas = (cost * 1.15).toFixed(0);
        const topAlacak = core.db.customers.reduce((a,m)=>{const b=core.musteriBalans(m.id);return a+(b>0?b:0);},0);
        document.getElementById('rapor-totals').innerHTML = `
            <div class="stat-card" style="border-bottom-color:var(--sekonder);"><small>TOPLAM CİRO</small><h2>${rev.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--danger);"><small>TOPLAM MALİYET</small><h2 style="color:var(--danger);">${cost.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--success);"><small>BRÜT KÂR / MARJ</small><h2 style="color:var(--success);">${profit.toLocaleString('tr-TR')} TL <small style="font-size:14px;">(%${margin})</small></h2></div>
            <div class="stat-card" style="border-bottom-color:var(--warn);"><small>TOPLAM ALACAK</small><h2 style="color:var(--warn);">${topAlacak.toLocaleString('tr-TR')} TL</h2></div>
        `;
        const fatRev = sales.filter(s=>s.evrak==='Fatura').reduce((a,b)=>a+b.tutar,0);
        const irsRev = sales.filter(s=>s.evrak==='İrsaliye').reduce((a,b)=>a+b.tutar,0);
        const nakitRev = sales.filter(s=>s.evrak==='Nakit').reduce((a,b)=>a+b.tutar,0);
        const topIskonto = sales.reduce((a,s)=>{const brut=s.satisFiyat*s.miktar;return a+(brut-s.tutar);},0);
        document.getElementById('rapor-financials').innerHTML = `
            <div class="glass-panel" style="margin-top:0;">
                <h4 style="margin-bottom:12px;color:var(--primer);">📊 Evrak Bazlı Dağılım</h4>
                <div style="display:flex;flex-direction:column;gap:12px;">
                    ${[['Fatura',fatRev,'#2E7D32'],['İrsaliye',irsRev,'#E65100'],['Nakit Satış',nakitRev,'#6F4E37']].map(([label,val,color])=>`
                    <div><div style="display:flex;justify-content:space-between;font-size:13px;margin-bottom:4px;"><span>${label}</span><b>${val.toLocaleString('tr-TR')} TL</b></div>
                    <div class="prog-bar"><div class="prog-fill" style="width:${rev>0?(val/rev*100):0}%;background:${color};"></div></div></div>`).join('')}
                </div>
            </div>
            <div class="glass-panel" style="margin-top:0;">
                <h4 style="margin-bottom:12px;color:var(--primer);">💡 Finansal Özet</h4>
                <table style="font-size:13px;">
                    <tr><td style="padding:6px 0;color:#888;">Başabaş Noktası:</td><td><b>${parseInt(basabas).toLocaleString('tr-TR')} TL</b></td></tr>
                    <tr><td style="padding:6px 0;color:#888;">Toplam Verilen İskonto:</td><td><b style="color:var(--danger);">${topIskonto.toLocaleString('tr-TR')} TL</b></td></tr>
                    <tr><td style="padding:6px 0;color:#888;">Ortalama Sipariş Değeri:</td><td><b>${sales.length>0?(rev/sales.length).toFixed(0):0} TL</b></td></tr>
                    <tr><td style="padding:6px 0;color:#888;">Tahsil Edilen:</td><td><b style="color:var(--success);">${core.db.cariHarekets.filter(c=>c.tur==='tahsilat').reduce((a,b)=>a+b.tutar,0).toLocaleString('tr-TR')} TL</b></td></tr>
                    <tr><td style="padding:6px 0;color:#888;">Toplam Ödemelerimiz:</td><td><b>${core.db.cariHarekets.filter(c=>c.tur==='odeme').reduce((a,b)=>a+b.tutar,0).toLocaleString('tr-TR')} TL</b></td></tr>
                </table>
            </div>
        `;
        document.getElementById('rapor-table').innerHTML = sales.map(s => {
            const maliyetT = s.alisF*s.miktar;
            const kar = s.tutar-maliyetT;
            const indirim = (s.satisFiyat*s.miktar) - s.tutar;
            return `<tr>
                <td>${s.tarih}</td>
                <td>${s.mAd}</td>
                <td>${s.uAd}</td>
                <td><span class="badge badge-${s.evrak==='Fatura'?'fatura':s.evrak==='İrsaliye'?'irsaliye':'evrak'}">${s.evrak}</span></td>
                <td style="color:var(--danger);">${indirim>0?indirim.toLocaleString('tr-TR')+' TL':'—'}</td>
                <td>${maliyetT.toLocaleString('tr-TR')} TL</td>
                <td><b>${s.tutar.toLocaleString('tr-TR')} TL</b></td>
                <td style="color:${kar>0?'green':'red'};font-weight:700;">${kar.toLocaleString('tr-TR')} TL</td>
            </tr>`;
        }).join('');
    },

    exportPDF: () => {
        const {jsPDF} = window.jspdf;
        const doc = new jsPDF();
        doc.setFontSize(18);
        doc.text('KAHVE ERP - SATIS RAPORU', 15, 20);
        doc.setFontSize(10);
        doc.text(`Tarih: ${new Date().toLocaleDateString('tr-TR')}`, 15, 28);
        const rows = core.db.sales.map(s => [s.tarih, s.mAd, s.uAd, s.evrak, s.tutar.toLocaleString('tr-TR')+' TL']);
        doc.autoTable({head:[['Tarih','Musteri','Urun','Evrak','Tutar']], body:rows, startY:35, styles:{fontSize:9}});
        doc.save(`ERP_Satis_${new Date().toLocaleDateString('tr-TR')}.pdf`);
    },

    exportPDFFull: () => {
        const {jsPDF} = window.jspdf;
        const doc = new jsPDF();
        const rev = core.db.sales.reduce((a,b)=>a+b.tutar, 0);
        const cost = core.db.sales.reduce((a,b)=>a+(b.alisF*b.miktar), 0);
        const profit = rev - cost;
        doc.setFontSize(20);
        doc.text('KAHVE ERP - FINANSAL RAPOR', 15, 20);
        doc.setFontSize(12);
        doc.text(`Rapor Tarihi: ${new Date().toLocaleDateString('tr-TR')}`, 15, 30);
        doc.text(`Toplam Ciro: ${rev.toLocaleString('tr-TR')} TL`, 15, 40);
        doc.text(`Toplam Maliyet: ${cost.toLocaleString('tr-TR')} TL`, 15, 48);
        doc.text(`Net Kar: ${profit.toLocaleString('tr-TR')} TL`, 15, 56);
        const rows = core.db.sales.map(s => [s.tarih, s.mAd, s.evrak, (s.alisF*s.miktar).toLocaleString()+' TL', s.tutar.toLocaleString()+' TL', (s.tutar-(s.alisF*s.miktar)).toLocaleString()+' TL']);
        doc.autoTable({head:[['Tarih','Musteri','Evrak','Maliyet','Satis','Net Kar']], body:rows, startY:65, styles:{fontSize:9}});
        doc.save(`ERP_FinansalRapor_${new Date().toLocaleDateString('tr-TR')}.pdf`);
    },

    exportRaporExcel: () => {
        const wb = XLSX.utils.book_new();
        const ws = XLSX.utils.json_to_sheet(core.db.sales.map(s=>({Tarih:s.tarih,Musteri:s.mAd,Urun:s.uAd,Evrak:s.evrak,Miktar:s.miktar,Satis_TL:s.tutar,Maliyet_TL:s.alisF*s.miktar,Net_Kar:s.tutar-(s.alisF*s.miktar)})));
        XLSX.utils.book_append_sheet(wb, ws, 'Satislar');
        XLSX.writeFile(wb, `FinansalRapor_${new Date().toLocaleDateString('tr-TR')}.xlsx`);
    },

    // ===================== ADMIN =====================
    openUserModal: () => { document.getElementById('mod-user').style.display='flex'; },

    saveUser: () => {
        const u = {
            user: document.getElementById('u-user').value.trim(),
            pass: document.getElementById('u-pass').value,
            role: document.getElementById('u-role').value,
            max: parseInt(document.getElementById('u-max').value)||10,
            excel: document.getElementById('u-excel').value === 'true',
            change: document.getElementById('u-change').value === 'true'
        };
        if(!u.user||!u.pass) { alert('Kullanıcı adı ve şifre zorunlu!'); return; }
        if(core.db.users.find(x=>x.user===u.user)) { alert('Bu kullanıcı adı zaten var!'); return; }
        u.firstLogin = true;
        core.db.users.push(u); core.save(); core.closeM();
    },

    delUser: (user) => {
        if(user==='admin') { alert('Admin kullanıcı silinemez!'); return; }
        if(confirm('Kullanıcıyı sil?')) { core.db.users = core.db.users.filter(u=>u.user!==user); core.save(); }
    },

    saveSettings: () => {
        if(core.user?.role !== 'admin') { alert('Bu ayarları sadece admin değiştirebilir!'); return; }
        core.db.settings = {
            stokEsik: parseInt(document.getElementById('set-stok-esik').value)||5,
            musteriEsik: parseInt(document.getElementById('set-musteri-esik').value)||20,
            deadlineEsik: parseInt(document.getElementById('set-deadline-esik').value)||2
        };
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        const msg = document.getElementById('ayar-msg');
        msg.innerText = '✅ Ayarlar başarıyla kaydedildi!';
        setTimeout(()=>{ msg.innerText=''; }, 3000);
        core.render();
    },

    renderAdmin: () => {
        document.getElementById('admin-tbody').innerHTML = core.db.users.map(u => `<tr>
            <td><b>${u.user}</b>${u.mustChangePass?'<span style="margin-left:8px;font-size:10px;background:#fff3e0;color:#E65100;padding:2px 8px;border-radius:8px;">Şifre sıfırlanacak</span>':''}</td>
            <td><span style="font-size:11px;font-weight:700;text-transform:uppercase;padding:3px 10px;border-radius:8px;background:${u.role==='admin'?'rgba(198,40,40,0.1)':'rgba(44,95,124,0.1)'};color:${u.role==='admin'?'#C62828':'#2C5F7C'};">${u.role}</span></td>
            <td>%${u.max}</td>
            <td>${u.excel?'✅ Var':'❌ Yok'}</td>
            <td>${u.change?'✅ Var':'❌ Yok'}</td>
            <td>${u.user!=='admin'?`<button class="btn btn-danger btn-sm" onclick="core.delUser('${u.user}')">Sil</button>`:''}</td>
        </tr>`).join('');
        if(core.db.settings) {
            document.getElementById('set-stok-esik').value = core.db.settings.stokEsik||5;
            document.getElementById('set-musteri-esik').value = core.db.settings.musteriEsik||20;
            document.getElementById('set-deadline-esik').value = core.db.settings.deadlineEsik||2;
        }
        const sel = document.getElementById('reset-user-sel');
        if(sel) {
            sel.innerHTML = '<option value="">-- Kullanıcı Seçin --</option>';
            core.db.users.filter(u=>u.user!=='admin').forEach(u => sel.innerHTML += `<option value="${u.user}">${u.user} (${u.role})</option>`);
        }
        // Admin tel yükle
        const telInp = document.getElementById('admin-tel-set');
        if(telInp && core.db.adminTel) telInp.value = core.db.adminTel;
    },

    // ===================== WHATSAPP =====================
    waMusteri: (mId) => {
        const m = core.db.customers.find(x=>x.id===mId);
        if(!m||!m.tel) { alert('Müşterinin telefon numarası yok!'); return; }
        const tel = m.tel.replace(/\D/g,'');
        const bugun = new Date();
        const sonS = new Date(m.sonSatis||bugun);
        const gunF = Math.floor((bugun-sonS)/(1000*60*60*24));
        const kalan = Math.max(0, m.tuketim-(gunF*(m.tuketim/30))).toFixed(1);
        const msg = encodeURIComponent(`Merhaba ${m.ad},\n\nKayıtlarımıza göre kahve stoğunuz yaklaşık ${kalan} kg kalmış olabilir.\n\nYeni sipariş oluşturmak ister misiniz? Size özel fırsatlarımızla hizmetinizdeyiz. ☕\n\nKahve ERP Sistemi`);
        window.open(`https://wa.me/${tel}?text=${msg}`, '_blank');
    },

    waZiyaret: (mId) => {
        const m = core.db.customers.find(x=>x.id===mId);
        if(!m||!m.tel) { alert('Müşterinin telefon numarası yok!'); return; }
        const tel = m.tel.replace(/\D/g,'');
        const msg = encodeURIComponent(`Merhaba ${m.ad},\n\nYakında sizi ziyaret etmeyi planlıyoruz. Size uygun bir zaman için randevu almak ister misiniz?\n\nİyi günler dileriz. ☕`);
        window.open(`https://wa.me/${tel}?text=${msg}`, '_blank');
    },

    // WA gönder VE uyarıyı dismiss et
    waDismiss: (mId, key) => {
        core.waMusteri(mId);
        if(key) core.dismissAlert(key);
    },

    // ===================== İŞLEMLER =====================
    openIslemModal: () => {
        const sel = document.getElementById('isl-musteri');
        sel.innerHTML = '<option value="">-- Seçin --</option>';
        core.db.customers.forEach(m => sel.innerHTML += `<option value="${m.id}">${m.ad}</option>`);
        document.getElementById('isl-deadline').value = new Date().toISOString().split('T')[0];
        document.getElementById('mod-islem').style.display='flex';
    },

    saveIslem: () => {
        const baslik = document.getElementById('isl-baslik').value.trim();
        if(!baslik) { alert('İşlem başlığı zorunlu!'); return; }
        const mId = parseInt(document.getElementById('isl-musteri').value)||null;
        const m = mId ? core.db.customers.find(x=>x.id===mId) : null;
        const isl = {
            id: Date.now(), baslik, mId, mAd: m?m.ad:'',
            tur: document.getElementById('isl-tur').value,
            oncelik: document.getElementById('isl-oncelik').value,
            deadline: document.getElementById('isl-deadline').value,
            aciklama: document.getElementById('isl-aciklama').value,
            durum: 'bekleyen',
            olusturma: new Date().toISOString().split('T')[0]
        };
        core.db.islemler.unshift(isl);
        // Uyarı dismissed listesinden temizle (yeni işlem eklendi)
        core.save(); core.closeM();
    },

    tamamlaIslem: (id) => {
        const idx = core.db.islemler.findIndex(x=>x.id===id);
        if(idx>-1) { core.db.islemler[idx].durum='tamamlandi'; core.db.islemler[idx].tamamlanma=new Date().toISOString().split('T')[0]; }
        // Dismissed listesinden çıkar ki bir daha görünmesin
        core.db.dismissedAlerts = (core.db.dismissedAlerts||[]).filter(k=>k!=='isl_'+id);
        core.save();
    },

    iptalIslem: (id) => {
        const idx = core.db.islemler.findIndex(x=>x.id===id);
        if(idx>-1) { core.db.islemler[idx].durum='iptal'; }
        core.save();
    },

    delIslem: (id) => {
        if(confirm('İşlemi sil?')) { core.db.islemler = core.db.islemler.filter(x=>x.id!==id); core.save(); }
    },

    dismissAlert: (key) => {
        if(!core.db.dismissedAlerts) core.db.dismissedAlerts=[];
        if(!core.db.dismissedAlerts.includes(key)) core.db.dismissedAlerts.push(key);
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        core.render();
    },

    filterIslem: (f, btn) => {
        document.querySelectorAll('.islem-tab').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core.renderIslemDash(f);
    },

    filterIslemPage: (f, btn) => {
        document.querySelectorAll('.islem-page-tab').forEach(b=>b.classList.remove('active'));
        if(btn) btn.classList.add('active');
        core.renderIslemlerPage(f);
    },

    _islemColor: (oncelik) => ({acil:'#C62828',yuksek:'#E65100',normal:'#2C5F7C',dusuk:'#388E3C'}[oncelik]||'#888'),
    _islemEmoji: (tur) => ({sevkiyat:'🚚',siparis:'📋',tahsilat:'💰',numune:'🧪',teklif:'📄',bakim:'🔧',diger:'📌'}[tur]||'📌'),
    _oncelikLabel: (o) => ({acil:'🔴 ACİL',yuksek:'🟠 YÜKSEK',normal:'🟡 NORMAL',dusuk:'🟢 DÜŞÜK'}[o]||o),

    renderIslemDash: (filter='bekleyen') => {
        const el = document.getElementById('islem-listesi');
        if(!el) return;
        let data = [...core.db.islemler];
        if(filter!=='tum') data = data.filter(x=>x.durum===filter);
        const today = new Date();
        data.sort((a,b)=>{
            const op={acil:0,yuksek:1,normal:2,dusuk:3};
            return (op[a.oncelik]||4)-(op[b.oncelik]||4);
        });
        if(!data.length) { el.innerHTML=`<div style="text-align:center;padding:2rem;color:#ccc;font-size:13px;">Bu kategoride işlem yok.</div>`; return; }
        el.innerHTML = data.slice(0,20).map(isl => {
            const dl = isl.deadline ? new Date(isl.deadline) : null;
            const dlDiff = dl ? Math.floor((dl-today)/(1000*60*60*24)) : null;
            const gecikti = dlDiff!==null && dlDiff<0 && isl.durum==='bekleyen';
            const renk = core._islemColor(isl.oncelik);
            return `<div style="border-left:4px solid ${renk};background:${gecikti?'rgba(198,40,40,0.05)':'#fff'};border-radius:10px;padding:10px 12px;margin-bottom:8px;${gecikti?'animation:pulse 2s infinite;':''}">
                <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px;">
                    <div style="flex:1;">
                        <div style="font-size:12px;font-weight:700;color:${renk};margin-bottom:3px;">${core._islemEmoji(isl.tur)} ${isl.baslik}</div>
                        ${isl.mAd?`<div style="font-size:11px;color:#888;">👤 ${isl.mAd}</div>`:''}
                        <div style="font-size:11px;color:${gecikti?'red':'#aaa'};margin-top:2px;">${gecikti?`🚨 ${Math.abs(dlDiff)} gün GECİKTİ`:dlDiff===0?'⏰ Son tarihi BUGÜN':dlDiff!==null?`📅 ${isl.deadline}`:''}</div>
                    </div>
                    <div style="display:flex;gap:4px;flex-shrink:0;">
                        ${isl.durum==='bekleyen'?`<button class="btn btn-success btn-sm" onclick="core.tamamlaIslem(${isl.id})" title="Tamamla">✅</button>`:''}
                        <button class="btn btn-danger btn-sm" onclick="core.delIslem(${isl.id})" title="Sil">×</button>
                    </div>
                </div>
            </div>`;
        }).join('');
    },

    renderIslemlerPage: (filter='tum') => {
        const today = new Date();
        const all = core.db.islemler;
        const bekleyen = all.filter(x=>x.durum==='bekleyen');
        const gecikti = bekleyen.filter(x=>x.deadline && new Date(x.deadline)<today);
        const tamamlandi = all.filter(x=>x.durum==='tamamlandi');
        document.getElementById('islem-stats').innerHTML = `
            <div class="stat-card" style="border-bottom-color:var(--warn);"><small>⏳ BEKLEYEN</small><h2>${bekleyen.length}</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--danger);"><small>🚨 GECİKEN</small><h2 style="color:red;">${gecikti.length}</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--success);"><small>✅ TAMAMLANAN</small><h2 style="color:green;">${tamamlandi.length}</h2></div>
        `;
        let data = [...all];
        if(filter==='bekleyen') data = data.filter(x=>x.durum==='bekleyen');
        else if(filter==='tamamlandi') data = data.filter(x=>x.durum==='tamamlandi');
        else if(filter==='iptal') data = data.filter(x=>x.durum==='iptal');
        data.sort((a,b)=>{const op={acil:0,yuksek:1,normal:2,dusuk:3};return (op[a.oncelik]||4)-(op[b.oncelik]||4);});
        const grid = document.getElementById('islem-page-grid');
        if(!data.length) { grid.innerHTML=`<div style="grid-column:1/-1;text-align:center;padding:3rem;color:#aaa;">Bu kategoride işlem yok.</div>`; return; }
        grid.innerHTML = data.map(isl => {
            const dl = isl.deadline ? new Date(isl.deadline) : null;
            const dlDiff = dl ? Math.floor((dl-today)/(1000*60*60*24)) : null;
            const gecikti = dlDiff!==null && dlDiff<0 && isl.durum==='bekleyen';
            const renk = core._islemColor(isl.oncelik);
            return `<div class="glass-panel" style="margin-top:0;border-top:4px solid ${renk};${gecikti?'background:rgba(198,40,40,0.04);':''}">
                <div style="display:flex;justify-content:space-between;margin-bottom:10px;">
                    <span style="font-size:20px;">${core._islemEmoji(isl.tur)}</span>
                    <span style="font-size:11px;font-weight:700;color:${renk};background:${renk}22;padding:3px 10px;border-radius:20px;">${core._oncelikLabel(isl.oncelik)}</span>
                </div>
                <div style="font-weight:700;font-size:13px;margin-bottom:6px;color:#222;">${isl.baslik}</div>
                ${isl.mAd?`<div style="font-size:12px;color:#888;margin-bottom:6px;">👤 ${isl.mAd}</div>`:''}
                ${isl.aciklama?`<div style="font-size:12px;color:#666;margin-bottom:8px;background:#f8f8f8;padding:8px;border-radius:8px;">${isl.aciklama}</div>`:''}
                <div style="font-size:11px;color:${gecikti?'red':dlDiff===0?'orange':'#aaa'};font-weight:${gecikti||dlDiff===0?'700':'400'};margin-bottom:10px;">
                    ${gecikti?`🚨 ${Math.abs(dlDiff)} gün GECİKTİ!`:dlDiff===0?'⏰ Son tarihi BUGÜN!':isl.deadline?`📅 ${isl.deadline}`:'—'}
                </div>
                <div style="display:flex;justify-content:space-between;align-items:center;">
                    <span style="font-size:11px;font-weight:700;padding:3px 10px;border-radius:20px;background:${isl.durum==='bekleyen'?'rgba(230,81,0,0.1)':isl.durum==='tamamlandi'?'rgba(46,125,50,0.1)':'rgba(0,0,0,0.05)'};color:${isl.durum==='bekleyen'?'#E65100':isl.durum==='tamamlandi'?'#2E7D32':'#888'};">${isl.durum.toUpperCase()}</span>
                    <div style="display:flex;gap:6px;">
                        ${isl.durum==='bekleyen'?`<button class="btn btn-success btn-sm" onclick="core.tamamlaIslem(${isl.id})">✅ Tamamla</button><button class="btn btn-sm" style="background:#eee;" onclick="core.iptalIslem(${isl.id})">❌ İptal</button>`:''}
                        <button class="btn btn-danger btn-sm" onclick="core.delIslem(${isl.id})">Sil</button>
                    </div>
                </div>
            </div>`;
        }).join('');
    },

    // ===================== SABAH UYARISI =====================
    morningAlertCheck: () => {
        const today = new Date().toISOString().split('T')[0];
        if(core.db.lastAlertDate === today) return; // Bugün zaten gönderildi
        const alerts = core.checkAlerts();
        const gecikenisl = core.db.islemler.filter(x => {
            if(x.durum!=='bekleyen'||!x.deadline) return false;
            return new Date(x.deadline) < new Date();
        });
        if(alerts.length===0 && gecikenisl.length===0) return;
        // Admin'e sabah uyarısı WA mesajı oluştur
        if(core.db.adminTel) {
            let msg = `☕ *KAHVE ERP SABAH RAPORU* — ${today}\n\n`;
            if(gecikenisl.length) {
                msg += `🚨 *GECİKEN İŞLEMLER (${gecikenisl.length}):*\n`;
                gecikenisl.forEach(x => { msg += `  • ${x.baslik} — ${x.deadline}\n`; });
                msg += '\n';
            }
            const kritikStok = core.db.products.filter(p=>p.stok<=p.min);
            if(kritikStok.length) {
                msg += `📦 *KRİTİK STOK (${kritikStok.length}):*\n`;
                kritikStok.forEach(p => { msg += `  • ${p.ad}: ${p.stok} adet\n`; });
                msg += '\n';
            }
            const mustStok = alerts.filter(a=>a.type==='orange'&&a.waId);
            if(mustStok.length) {
                msg += `🏪 *MÜŞTERİ STOK UYARISI (${mustStok.length}):*\n`;
                mustStok.forEach(a => { msg += `  • ${a.rawMsg||a.msg.replace(/<[^>]*>/g,'')}\n`; });
            }
            msg += `\n📊 Sisteme giriş yapın: Kahve ERP Ultra Pro`;
            const tel = core.db.adminTel.replace(/\D/g,'');
            const encoded = encodeURIComponent(msg);
            // Sabah uyarısını bir popup ile göster
            setTimeout(() => {
                if(confirm(`📱 ${gecikenisl.length} geciken işlem ve ${kritikStok.length} kritik stok uyarısı var!\n\nAdmin'e WhatsApp sabah raporu gönderilsin mi?`)) {
                    window.open(`https://wa.me/${tel}?text=${encoded}`, '_blank');
                }
            }, 1500);
        }
        core.db.lastAlertDate = today;
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
    },

    // ===================== ADMİN TELEFON =====================
    saveAdminTel: () => {
        const tel = document.getElementById('admin-tel-input').value.replace(/\D/g,'');
        if(tel.length < 10) { alert('Geçerli bir telefon numarası girin!'); return; }
        core.db.adminTel = tel;
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        document.getElementById('mod-admin-tel').style.display='none';
        core.render();
    },

    saveAdminTelAdmin: () => {
        const tel = document.getElementById('admin-tel-set').value.replace(/\D/g,'');
        if(tel.length < 10) { alert('Geçerli bir telefon numarası girin!'); return; }
        core.db.adminTel = tel;
        localStorage.setItem('erp_ultra_premium_v3', JSON.stringify(core.db));
        const m = document.getElementById('admin-tel-msg');
        m.innerText = '✅ Numara kaydedildi: +' + tel;
        setTimeout(()=>{ m.innerText=''; }, 3000);
    },

    waAdminTest: () => {
        if(!core.db.adminTel) { alert('Önce telefon numarası kaydedin!'); return; }
        const tel = core.db.adminTel.replace(/\D/g,'');
        const msg = encodeURIComponent(`☕ Kahve ERP Test Mesajı\n\nSistem uyarıları bu numaraya gönderilecek. Sabah raporları ve kritik uyarılar için bu hat aktif edilmiştir.\n\nKahve ERP Ultra Pro ✅`);
        window.open(`https://wa.me/${tel}?text=${msg}`, '_blank');
    },

    // ===================== SMART ALERTS =====================
    checkAlerts: () => {
        const alerts = [];
        const today = new Date();
        const todayStr = today.toISOString().split('T')[0];
        const esik = core.db.settings?.musteriEsik||20;
        const dlEsik = core.db.settings?.deadlineEsik||2;
        const dismissed = core.db.dismissedAlerts||[];

        // Geciken İşlemler uyarıları
        core.db.islemler.filter(x=>x.durum==='bekleyen'&&x.deadline).forEach(isl => {
            const dl = new Date(isl.deadline);
            const diff = Math.floor((dl-today)/(1000*60*60*24));
            const key = 'isl_'+isl.id+'_'+isl.deadline;
            if(dismissed.includes(key)) return;
            if(diff < 0) {
                alerts.push({type:'red', key, msg:`🚨 GECİKEN İŞLEM: <b>${isl.baslik}</b> — ${Math.abs(diff)} gün gecikmiş!`, islemId:isl.id});
            } else if(diff === 0) {
                alerts.push({type:'red', key, msg:`⏰ BUGÜN SON: <b>${isl.baslik}</b> — bugün tamamlanmalı!`, islemId:isl.id});
            } else if(diff <= dlEsik) {
                alerts.push({type:'orange', key, msg:`📋 Yaklaşan İşlem: <b>${isl.baslik}</b> — ${diff} gün kaldı.`, islemId:isl.id});
            }
        });

        // Müşteri stok uyarıları
        core.db.customers.forEach(m => {
            const key = 'mstok_'+m.id+'_'+todayStr;
            if(dismissed.includes(key)) return;
            const sonS = new Date(m.sonSatis||today);
            const gunF = Math.max(0, Math.floor((today-sonS)/(1000*60*60*24)));
            const kalan = Math.max(0, m.tuketim-(gunF*(m.tuketim/30)));
            const oran = m.tuketim>0?(kalan/m.tuketim)*100:100;
            if(oran < esik) {
                alerts.push({type:'orange', key, rawMsg:`${m.ad} stoğu bitmek üzere (${kalan.toFixed(1)} Kg kaldı)`, msg:`📦 <b>${m.ad}</b> stoğu bitmek üzere! (Tahmini kalan: ${kalan.toFixed(1)} Kg)`, waId:m.id});
            }
        });

        // Deadline uyarıları (ziyaret)
        core.db.visits.forEach(v => {
            if(!v.deadline||v.durum==='tamamlandi') return;
            const key = 'ziyaret_'+v.id+'_'+v.deadline;
            if(dismissed.includes(key)) return;
            const dl = new Date(v.deadline);
            const diff = Math.floor((dl-today)/(1000*60*60*24));
            if(diff < 0) {
                alerts.push({type:'red', key, msg:`🗓️ GECİKME: <b>${v.mAd}</b> için "${v.aksiyon||v.amac}" ${Math.abs(diff)} gün gecikmiş!`});
            } else if(diff <= dlEsik) {
                alerts.push({type:'orange', key, msg:`⏰ <b>${v.mAd}</b> için "${v.aksiyon||v.amac}" son tarihi ${diff===0?'BUGÜN':`${diff} gün sonra`}!`});
            }
        });

        // Kritik stok uyarıları
        core.db.products.forEach(p => {
            const key = 'ustok_'+p.id;
            if(dismissed.includes(key)) return;
            if(p.stok <= p.min) alerts.push({type:'red', key, msg:`🏭 Kritik Stok: <b>${p.ad}</b> — sadece ${p.stok} adet kaldı! (Min: ${p.min})`});
        });

        return alerts;
    },

    // ===================== RENDER MAIN =====================
    render: () => {
        // Smart alerts
        const alerts = core.checkAlerts();
        document.getElementById('smart-alerts').innerHTML = alerts.length ? alerts.map(a => `
            <div class="alert-bar alert-${a.type}" style="position:relative;">
                <span style="flex:1;">${a.msg}</span>
                ${a.waId?`<button class="btn btn-wa btn-sm" onclick="core.waDismiss(${a.waId},'${a.key||''}')">📱 WA Gönder & Kapat</button>`:''}
                ${a.islemId?`<button class="btn btn-success btn-sm" onclick="core.tamamlaIslem(${a.islemId})">✅ Tamamla</button>`:''}
                <button onclick="core.dismissAlert('${a.key||''}')" style="background:none;border:none;cursor:pointer;font-size:16px;color:inherit;margin-left:4px;opacity:0.6;padding:2px 6px;" title="Uyarıyı kapat">✕</button>
            </div>`).join('') : `<div class="alert-bar alert-blue">✅ Tüm sistemler normal — aktif uyarı yok.</div>`;
        const alertCount = alerts.filter(a=>a.type==='red').length;
        const ac = document.getElementById('alert-count');
        if(alertCount > 0) { ac.style.display='flex'; document.getElementById('alert-num').innerText=alertCount; }
        else { ac.style.display='none'; }

        // Dashboard Stats
        const rev = core.db.sales.reduce((a,b)=>a+b.tutar, 0);
        const cost = core.db.sales.reduce((a,b)=>a+(b.alisF*b.miktar), 0);
        const profit = rev - cost;
        const topAlacak = core.db.customers.reduce((a,m)=>{const b=core.musteriBalans(m.id);return a+(b>0?b:0);},0);
        document.getElementById('dash-stats').innerHTML = `
            <div class="stat-card"><small>TOPLAM CİRO</small><h2>${rev.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--success);"><small>NET KARLILIK</small><h2 style="color:var(--success);">+${profit.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--warn);"><small>TOPLAM ALACAK</small><h2 style="color:var(--warn);">${topAlacak.toLocaleString('tr-TR')} TL</h2></div>
            <div class="stat-card" style="border-bottom-color:var(--sekonder);"><small>AKTİF MÜŞTERİ</small><h2>${core.db.customers.length}</h2></div>
        `;

        // Dashboard son satışlar
        const dashSales = document.querySelector('#dash-last-sales tbody');
        if(dashSales) dashSales.innerHTML = core.db.sales.slice(0,8).map(s => `<tr><td>${s.tarih}</td><td>${s.mAd}</td><td><b>${s.tutar.toLocaleString()} TL</b></td><td><span class="badge badge-${s.evrak==='Fatura'?'fatura':s.evrak==='İrsaliye'?'irsaliye':'evrak'}">${s.evrak}</span></td></tr>`).join('');

        // Dashboard kritik stok
        const lowStock = core.db.products.filter(p=>p.stok<=p.min);
        document.getElementById('dash-low-stock').innerHTML = lowStock.length?lowStock.map(p=>`<div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #f0f0f0;font-size:13px;"><span>${p.ad}</span><b style="color:red;">${p.stok} adet</b></div>`).join(''):'<p style="color:green;font-size:13px;padding:10px 0;">✅ Tüm stoklar yeterli.</p>';

        // Dashboard müşteri stok uyarıları
        const mustAlerts = core.checkAlerts().filter(a=>a.type==='orange'&&a.waId);
        document.getElementById('dash-customer-alerts').innerHTML = mustAlerts.length?mustAlerts.map(a=>`<div style="font-size:12px;padding:8px 0;border-bottom:1px solid #f0f0f0;display:flex;justify-content:space-between;align-items:center;">${a.msg} <button class="btn btn-wa btn-sm" onclick="core.waDismiss(${a.waId},'${a.key||''}')">📱</button></div>`).join(''):'<p style="color:green;font-size:13px;padding:10px 0;">✅ Stok uyarısı yok.</p>';

        // Dashboard işlemler
        core.renderIslemDash('bekleyen');

        // Render active page content
        const activePage = document.querySelector('.page.active');
        if(activePage) {
            const id = activePage.id.replace('page-','');
            if(id!=='dashboard') core.renderPage(id);
        }
        // Always render satis and urun lists if on those pages
        core.renderSatis('tum');
        core.renderUrunler('tum');
        core.renderZiyaret('tum');
        core.renderMusteriler();
    },

    closeM: () => { document.querySelectorAll('.modal-bg').forEach(m => m.style.display='none'); }
};

// CSS animations
const style = document.createElement('style');
style.textContent = `
@keyframes shake{0%,100%{transform:translateX(0);}25%{transform:translateX(-10px);}75%{transform:translateX(10px);}}
@keyframes pulse{0%,100%{opacity:1;}50%{opacity:0.6;}}
`;document.head.appendChild(style);

core.init();
</script>
<!-- İŞLEM EKLEME MODALI -->
<div class="modal-bg" id="mod-islem">
    <div class="modal-sm">
        <h3 style="margin-bottom:2rem;color:var(--sekonder);">⚡ Yeni İşlem Kaydı</h3>
        <div class="fg">
            <label>İşlem Başlığı</label>
            <input type="text" id="isl-baslik" placeholder="Örn: Demir Otel — 20 Kg Sipariş Gönderilecek">
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:15px;">
            <div class="fg">
                <label>Müşteri (isteğe bağlı)</label>
                <select id="isl-musteri"><option value="">-- Seçin --</option></select>
            </div>
            <div class="fg">
                <label>Öncelik</label>
                <select id="isl-oncelik">
                    <option value="acil">🔴 Acil</option>
                    <option value="yuksek">🟠 Yüksek</option>
                    <option value="normal" selected>🟡 Normal</option>
                    <option value="dusuk">🟢 Düşük</option>
                </select>
            </div>
            <div class="fg">
                <label>İşlem Türü</label>
                <select id="isl-tur">
                    <option value="sevkiyat">🚚 Mal Gönderimi / Sevkiyat</option>
                    <option value="siparis">📋 Sipariş Talebi</option>
                    <option value="tahsilat">💰 Tahsilat</option>
                    <option value="numune">🧪 Numune Gönderimi</option>
                    <option value="teklif">📄 Teklif Hazırlama</option>
                    <option value="bakim">🔧 Bakım / Servis</option>
                    <option value="diger">📌 Diğer</option>
                </select>
            </div>
            <div class="fg">
                <label>Son Tarih (Deadline)</label>
                <input type="date" id="isl-deadline">
            </div>
        </div>
        <div class="fg">
            <label>Açıklama / Detay</label>
            <textarea id="isl-aciklama" rows="2" style="resize:none;" placeholder="İşlem detaylarını yazın..."></textarea>
        </div>
        <div style="display:flex;gap:10px;margin-top:1rem;">
            <button class="btn btn-secondary" style="flex:1;justify-content:center;padding:15px;" onclick="core.saveIslem()">💾 İŞLEM KAYDET</button>
            <button class="btn btn-danger" onclick="core.closeM()">İPTAL</button>
        </div>
    </div>
</div>

<!-- ADMİN TELEFON ZORUNLU MODAL -->
<div class="modal-bg" id="mod-admin-tel" style="z-index:9500;">
    <div class="modal-sm" style="text-align:center;">
        <div style="font-size:48px;margin-bottom:10px;">📱</div>
        <h3 style="color:var(--primer);margin-bottom:1rem;">Admin WhatsApp Numarası</h3>
        <p style="color:#888;font-size:13px;margin-bottom:2rem;line-height:1.7;">Sistem uyarıları ve günlük rapor için admin'in WhatsApp numarasını girin. Bu numara sabah uyarı mesajları ve kritik bildirimleri için kullanılacaktır.</p>
        <div class="fg" style="text-align:left;">
            <label>Telefon Numarası (Ülke kodu dahil)</label>
            <input type="text" id="admin-tel-input" placeholder="905xxxxxxxxx" style="border:1.5px solid #ddd;border-radius:12px;padding:14px;font-size:16px;text-align:center;letter-spacing:2px;">
            <small style="color:#aaa;font-size:11px;">Örnek: 905321234567 (başında + olmadan)</small>
        </div>
        <button class="btn btn-primary" style="width:100%;justify-content:center;padding:16px;font-size:15px;margin-top:1rem;" onclick="core.saveAdminTel()">✅ Numarayı Kaydet ve Devam Et</button>
        <div style="margin-top:1rem;font-size:11px;color:#ccc;">Admin panelinden daha sonra değiştirilebilir</div>
    </div>
</div>

</body>
</html>
